Date: 2026-09-20
TaskStatus: not_completed

Task scope:
  开工：开工前核对设计 → F1.1（W0 对账工具与黄金基线）→ F3.1 第二批静态稿（并行）→ 之后 F1.2 / F1.3。

Current summary:
  - 开工前核对完成，4 个问题已答复（评审记录 §9）：
      D33 「执行出错」取宽口径：任何一次调用重试用尽或一路解码失败 → 整条 error、待补跑
      D34 冻结点 release_v1@45bdf9292；之后分批同步，新格式（#155 mcap/lance）按 .rrd 办法处理
      基线在现网 v1 的 Pod 里生成（导出脚本自带冻结点的 v1 源码）
      需求方已同意删除上游 daft 源码；第一批静态稿风格已确认
  - 文档已按上述决策修订，并修掉三处「保存密钥时做写探针」的过时说法与 00 §7 表格断行
  - 进行中：F1.1 W0 工具（离线部分）、F3.1 第二批静态稿

Main gaps:
  - 生成基线需要：两个数据集的 tos:// 地址和地域、存档放哪个 TOS 目录、现网站点配置（CURATION_CONFIG 非密钥部分）
  - 方舟 GET {endpoint}/models 用 API Key 是否可用仍未实测（不阻塞）
  - 02 §3.6/§3.8 的 --revision 参数留给 W2 冻结 C2 时补

Next steps:
  1. W0：规范化记录 schema + 请求哈希 + 录制/回放 + dump_v1 + curation-parity compare + 迷你数据集夹具与测试
  2. 把 W0 的 Pod 内执行步骤写进 README，交给用户在现网 Pod 生成两个基线
  3. 基线生成期间推进 F1.2（W1 目录重组）与 F1.3（W2 契约冻结）
