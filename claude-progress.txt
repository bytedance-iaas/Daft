Date: 2026-09-21
TaskStatus: not_completed

Task scope:
  开工：开工前核对设计 → F1.1（W0 对账工具与黄金基线）→ F1.2（W1 清理与重组）→ F3.1 第二批静态稿（并行）→ F1.3（W2 契约冻结）
  → 契约核对（D35）→ 批次 1：W3 前半（F2.1 骨架、F2.7）、W4（F2.2）、W6（F2.4 不碰 B 类的部分）、W7（F2.5）。

Current summary:
  - 开工前核对完成（评审记录 §9）：D33 执行出错取宽口径；D34 冻结点 release_v1@45bdf9292；基线在现网 Pod 里生成；
    需求方同意删除上游 daft；第一批静态稿风格已确认。飞书副本已同步（rev 65）。
  - F1.1 / W0 工具完成（tools/parity/，用法见同目录 README.md）：dump-v1（挂钩导出 v1 结果，录制/回放/补录模型调用）、
    compare（六项逐位、三项判决级+噪声底、终判清单、回放命中；--all-strict）、make-fixture、tape-summary、pack、
    archive/fetch、v1-manifest、v1-src、a-class-check。契约：docs/contracts/cli/result-record.schema.json（verdict 增加 scored）、
    docs/contracts/parity/vlm-tape-entry.schema.json。58 个测试全过（含合成数据上的真跑：录制→回放逐位一致、123 次请求全部命中）。
  - F1.2 / W1 五步完成，各自独立 commit：
      ① 删除上游 daft（2636 个文件）；CLAUDE.md（原为指向 daft AGENTS.md 的符号链接）换成 Curator 说明
      ② 目录重组：backend/curation（测试留在包内）、deploy/Dockerfile（构建上下文改为仓库根）、docs/design、docs/contracts、
         frontend/、tools/parity；v1 使用文档归档 docs/v1/；本地 venv 挪到仓库根 .venv
      ③ 下线 Gradio 界面与内嵌终端；ui/ 里不依赖 Gradio 的 auth/runner/manifest/episode_detail/reaper 暂留作移植参考；
         134 条依赖界面的测试下线，逐条去向见 docs/v1/retired-ui-tests.md
      ④ 依赖去掉 gradio、websockets；Dockerfile 去掉字体补丁层
      ⑤ 新 CI（.github/workflows/ci.yml）：v1 单测、对账工具、A 类保护检查、镜像构建
    验证：v1 单测 1408 过（5 条是本机无 GPU / macOS 的已知失败）；CLI 正常；
          工作区 v1 回放冻结点录制带，九项逐位一致（重构未改变任何质检结果）；A 类 57 个文件与冻结点一致
  - 实测确认 v1 小问题：被去重剔除的条目仍在 passed.json 里（交付数据集正确），终判对账按交付口径比
  - F3.1 完成：第二批四页（任务详情、质检报告、人工裁决、密钥与资源管理）加前两页的评审修改，已合入 frontend/mockups/；
    六页无外部资源、无控制台报错、页面间链接齐全；README 列了手动核对步骤与待确认问题 1–10（进 F3.2 前需求方评审）
  - F1.3 / W2 契约冻结完成（docs/contracts/，说明见其 README）：
      C1 backend/curation/contracts/modules.py → modules.json（八模块、档、依赖、参数 schema、报告明细表与排序白名单；
         不放在 A 类 registry/ 目录）
      C2 docs/contracts/cli/ 20 份 schema（十条命令的 --json、parts/results 行、判决行、四份清单、commit.json、导出清单、
         report.json、裁决输入、错误信封）；C3 progress.schema.json；C4 openapi.yaml（39 路径 48 操作，报告/计划/预检引用 C2）；
      C5 backend/daemon/repo/protocol.py（实体、状态机迁移表、CAS、两种分页、错误类型）
      契约测试 backend/tests/contracts 70 条：Schema 合法、22 组示例（合法/不合法）、注册表一致性、OpenAPI 有效且覆盖 03 篇接口表、
      状态机与 01 篇迁移表逐边一致、CONTRACTS.lock 漂移检测（手动改契约验证过：报出文件、退出码 1）
      与设计的出入已回写文档：新增 GET /tasks/{id}/timeline；证据模式取值用 v1 的 off；aggregate/report 补 --revision。
      更正：「硬门确定失败压过执行出错（drop 优先于 held）」只写进了 verdict-line 契约的说明，设计原文 02 §3.6、06 §3
      仍是「任一模块出错即进 held」，两处不一致，已列为待拍板（见下）
  - 2026-09-21 续：
      · 契约一页摘要 docs/contracts/SUMMARY.md（要点、冻结时定下的 7 处细节、1 处待拍板），用户已评审：7 处全部接受，那 1 处定为 D35
      · 修 openapi.yaml 错误体 message 描述的 YAML 解析事故（逗号未加引号多出一个字段），加回归测试，锁已刷新；
        契约测试 71 条全过（51e048568）
      · 推送：origin = github.com/bytedance-iaas/daft 是公开仓库，用户确认可以直接推；已推 feat/curator-v2（2026-09-21）
      · D35 已拍板并落地（77741a56f）：已被正常判完的模块确定拒绝的条目不进 held；决策表、00–04/06/11 篇、判决行契约、
        评审记录 §10、账本修订记录；飞书副本已同步（revision 130，头部改为 docs/design/ 与 D1–D35）
      · 11 §6：planner 库与合并框架放 backend/curation/planner/（CLI 的 plan 与 Daemon 共用）；并行 agent 不改公共文件
      · CI：run #1 镜像构建与 A 类保护通过，tests 在 v1 单测一步失败（日志要登录 GitHub 才能看，gh 查询被权限拦下）；
        加了 pytest-github-actions-annotate-failures，失败用例以注解形式显示在公开的运行页上（ba9fc7307）。
        run #2 的注解显示是 3 条 v1 测试：在冻结点源码上同样不过，原因都在测试本身（FSX 两条的模拟方式与 09-04 起的判据不符、
        看门狗那条子进程 PYTHONPATH 多退一级），已修正并记入 docs/v1/test-fixes.md（8d0c70af1）；本机 v1 单测 1407 过、0 失败。
        run #3 全绿（v1 单测、契约、对账工具、A 类保护、镜像构建）→ F1.2 置 completed
      · 批次 1 已派出 4 个后台 agent（各自 worktree）：W4 Daemon 骨架、W6 planner 与合并框架、W7 增量导出、W3 CLI 非 B 类部分
      · 需求方：黄金基线暂缓（v1 在 Pod 上跑过、结果正常，旧系统有备份）；W3 拆编排的卡点放宽，改由合成数据集上
        冻结点 v1 与 v2 的逐位对账守住（进 CI），真实数据基线在 F4.2 前补（11 §3、评审记录 §10、账本修订记录）

Main gaps:
  - F1.1 基线暂缓（需求方 2026-09-21 决定），F4.2 黄金对账之前按 tools/parity/README.md 在 Pod 里补
  - 批次 1 进行中，未合并
  - compare 目前只读导出目录；v2 运行目录的读取随 W3 补（行格式已由 C2 冻结）

Next steps:
  1. 批次 1 各 agent 交付后逐个合并：核对目录边界与 A/B 类纪律，跑契约与各包测试，落地它们报告里的公共文件改动
     （依赖、CI 步骤、README 手动验证步骤），更新 feature_list 状态
  2. W3 后半（卡点已放宽）：拆 v1 编排为 autolabel / check / aggregate / export / report / adjudicate-apply；
     compare 补 v2 运行目录的读取；合成数据集上冻结点 v1 对 v2 的 compare --all-strict 进 CI
  3. 批次 2：W5 任务编排、W8 密钥管理（都依赖 W4）
  4. 需求方评审六页静态稿（frontend/mockups/，README 待确认 1–10），通过后开 F3.2
  5. 真实数据的两个基线在 F4.2 黄金对账之前补
