Date: 2026-09-23
TaskStatus: not_completed

Task scope:
  开工：开工前核对设计 → F1.1（W0 对账工具与黄金基线）→ F1.2（W1 清理与重组）→ F3.1 第二批静态稿（并行）→ F1.3（W2 契约冻结）
  → 契约核对（D35）→ 批次 1：W3 前半（F2.1 骨架、F2.7）、W4（F2.2）、W6（F2.4 不碰 B 类的部分）、W7（F2.5）。

Current summary:
  - 开工前核对完成（评审记录 §9）：D33 执行出错取宽口径；D34 冻结点 release_v1@45bdf9292；基线在现网 Pod 里生成；
    需求方同意删除上游 daft；第一批静态稿风格已确认。飞书副本已同步（rev 65）。
  - F1.1 / W0 工具完成（tools/parity/，用法见同目录 README.md）：dump-v1（挂钩导出 v1 结果，录制/回放/补录模型调用）、
    compare（六项逐位、三项判决级+噪声底、终判清单、回放命中；--all-strict）、make-fixture、tape-summary、pack、
    archive/fetch、v1-manifest、v1-src、a-class-check。契约：docs/contracts/cli/result-record.schema.json（verdict 增加 scored）、
    docs/contracts/parity/vlm-tape-entry.schema.json。58 个测试全过（含合成数据上的真跑：录制→回放逐位一致、123 次请求全部命中）。
  - F1.2 / W1 五步完成，各自独立 commit：
      ① 删除上游 daft（2636 个文件）；CLAUDE.md（原为指向 daft AGENTS.md 的符号链接）换成 Curator 说明
      ② 目录重组：backend/curation（测试留在包内）、deploy/Dockerfile（构建上下文改为仓库根）、docs/design、docs/contracts、
         frontend/、tools/parity；v1 使用文档归档 docs/v1/；本地 venv 挪到仓库根 .venv
      ③ 下线 Gradio 界面与内嵌终端；ui/ 里不依赖 Gradio 的 auth/runner/manifest/episode_detail/reaper 暂留作移植参考；
         134 条依赖界面的测试下线，逐条去向见 docs/v1/retired-ui-tests.md
      ④ 依赖去掉 gradio、websockets；Dockerfile 去掉字体补丁层
      ⑤ 新 CI（.github/workflows/ci.yml）：v1 单测、对账工具、A 类保护检查、镜像构建
    验证：v1 单测 1408 过（5 条是本机无 GPU / macOS 的已知失败）；CLI 正常；
          工作区 v1 回放冻结点录制带，九项逐位一致（重构未改变任何质检结果）；A 类 57 个文件与冻结点一致
  - 实测确认 v1 小问题：被去重剔除的条目仍在 passed.json 里（交付数据集正确），终判对账按交付口径比
  - F3.1 完成：第二批四页（任务详情、质检报告、人工裁决、密钥与资源管理）加前两页的评审修改，已合入 frontend/mockups/；
    六页无外部资源、无控制台报错、页面间链接齐全；README 列了手动核对步骤与待确认问题 1–10（进 F3.2 前需求方评审）
  - F1.3 / W2 契约冻结完成（docs/contracts/，说明见其 README）：
      C1 backend/curation/contracts/modules.py → modules.json（八模块、档、依赖、参数 schema、报告明细表与排序白名单；
         不放在 A 类 registry/ 目录）
      C2 docs/contracts/cli/ 20 份 schema（十条命令的 --json、parts/results 行、判决行、四份清单、commit.json、导出清单、
         report.json、裁决输入、错误信封）；C3 progress.schema.json；C4 openapi.yaml（39 路径 48 操作，报告/计划/预检引用 C2）；
      C5 backend/daemon/repo/protocol.py（实体、状态机迁移表、CAS、两种分页、错误类型）
      契约测试 backend/tests/contracts 70 条：Schema 合法、22 组示例（合法/不合法）、注册表一致性、OpenAPI 有效且覆盖 03 篇接口表、
      状态机与 01 篇迁移表逐边一致、CONTRACTS.lock 漂移检测（手动改契约验证过：报出文件、退出码 1）
      与设计的出入已回写文档：新增 GET /tasks/{id}/timeline；证据模式取值用 v1 的 off；aggregate/report 补 --revision。
      更正：「硬门确定失败压过执行出错（drop 优先于 held）」只写进了 verdict-line 契约的说明，设计原文 02 §3.6、06 §3
      仍是「任一模块出错即进 held」，两处不一致，已列为待拍板（见下）
  - 2026-09-21 续：
      · 契约一页摘要 docs/contracts/SUMMARY.md（要点、冻结时定下的 7 处细节、1 处待拍板），用户已评审：7 处全部接受，那 1 处定为 D35
      · 修 openapi.yaml 错误体 message 描述的 YAML 解析事故（逗号未加引号多出一个字段），加回归测试，锁已刷新；
        契约测试 71 条全过（51e048568）
      · 推送：origin = github.com/bytedance-iaas/daft 是公开仓库，用户确认可以直接推；已推 feat/curator-v2（2026-09-21）
      · D35 已拍板并落地（77741a56f）：已被正常判完的模块确定拒绝的条目不进 held；决策表、00–04/06/11 篇、判决行契约、
        评审记录 §10、账本修订记录；飞书副本已同步（revision 130，头部改为 docs/design/ 与 D1–D35）
      · 11 §6：planner 库与合并框架放 backend/curation/planner/（CLI 的 plan 与 Daemon 共用）；并行 agent 不改公共文件
      · CI：run #1 镜像构建与 A 类保护通过，tests 在 v1 单测一步失败（日志要登录 GitHub 才能看，gh 查询被权限拦下）；
        加了 pytest-github-actions-annotate-failures，失败用例以注解形式显示在公开的运行页上（ba9fc7307）。
        run #2 的注解显示是 3 条 v1 测试：在冻结点源码上同样不过，原因都在测试本身（FSX 两条的模拟方式与 09-04 起的判据不符、
        看门狗那条子进程 PYTHONPATH 多退一级），已修正并记入 docs/v1/test-fixes.md（8d0c70af1）；本机 v1 单测 1407 过、0 失败。
        run #3 全绿（v1 单测、契约、对账工具、A 类保护、镜像构建）→ F1.2 置 completed
      · 批次 1 已派出 4 个后台 agent（各自 worktree）：W4 Daemon 骨架、W6 planner 与合并框架、W7 增量导出、W3 CLI 非 B 类部分
      · 需求方：黄金基线暂缓（v1 在 Pod 上跑过、结果正常，旧系统有备份）；W3 拆编排的卡点放宽，改由合成数据集上
        冻结点 v1 与 v2 的逐位对账守住（进 CI），真实数据基线在 F4.2 前补（11 §3、评审记录 §10、账本修订记录）
      · 静态稿评审（2026-09-21）：六页通过，文案等页面全部实现后一次性改；D36 概览、数据集两页（数据集登记并记指纹）、
        D37 开始时核对指纹（不一致弹框确认后重新预检）、D38 新建任务两屏；其余答复落在 07 篇与静态稿 README 的「评审结论」；
        新接口写在 03 §12，契约随 1.1 修订补（a6cf86354）
      · 合并 W6（b016603a3）：backend/curation/planner/ 独立库（计划、N→八把闸门与 v1 出厂默认逐项相等、合并框架、
        Token 两本账、外层重试、自适应降并发），433 条测试；未接入 check / vlm_client（随 W3 后半）；F2.4 保持 not_completed
      · 合并 W3 前半（0ea167f60）：backend/curation/cli/（v1 命令行挪成 legacy.py，只改 import）、preflight、snapshot、
        verify、curation task 客户端，106 条测试；F2.1、F2.7 保持 not_completed（后半与 D37 未做）
      · 合并后：v2 测试 610 过（契约 71、CLI 106、planner 433），v1 单测 1407 过 0 失败，契约锁一致，A 类 57 个文件未动；
        CI 拆出 CLI 与 planner 两步；README 加两个包的目录与手动验证步骤
      · 契约 1.1 修订清单（D36/D37 的新接口 + W3、W6 报告的 13 处缺口）记在 docs/contracts/SUMMARY.md §五，等 W4、W7 合并后一次改
      · 契约 1.1 修订（59880b636）：C4 1.1.0（数据集登记与浏览、概览、开始时指纹核对与 repreflight、任务列表按模块 /
        数据集筛选、dataset_id 输入、dataset_in_use），C5 加 Dataset / DatasetCheck，C1 1.1（参数带表单标题与选项名），
        C2/C3 兼容扩充（预检 reason_code、退出码 1/7/8、指纹算法、合并回执口径、闸门与档位可扩展、调用种类开放、
        导出清单可选 task/files、videos_renamed/full_reason）；设计 01–06、09 跟进；预检 reason_code 已补进 CLI
      · 合并 W7（f8c9a2eaa）：backend/curation/export/ 增量重导出库（全量与 v1 逐字节一致，v2 改号视频只改名，
        v3 只重编码受影响文件，只改文本不碰视频，中断后自动退回全量）；官方 loader 在两个独立 venv 实跑通过
        （0.3.3 读 v2.1、0.6.1 读 v3.0，零警告）；CI 加 tests/export 步骤与 lerobot-loader job（带护栏：该格式的用例全跳过则失败）；
        CI run 9 全绿 → F2.5 置 completed
      · 修掉一条时过时不过的对账工具测试（改录制带里「第一条」打分答案，顺序受线程调度影响；3ee3b6b17）
      · 批次 2 已派出 W10 前端、W3 后半两个后台 agent（worktree 均从 0f8f8c4d0 起）；W4 仍在进行
      · 合并后 v2 测试 684 过、3 跳过（loader 用例在共享 venv 里跳过）
      · 13:40 PDT 前三个 agent 因会话额度（HTTP 429）中断；额度重置后用原上下文恢复，改为每一步都提交
      · 合并 W4（ac45eaac1）：Daemon 骨架（FastAPI、SQLite 仓储实现 C5、Basic 鉴权、SSE 重放、探针、SPA 回退、
        v1 深链 302、启动对账、同一数据卷单实例锁），独立评审 10 个问题已修；随后对齐契约 1.1（0a6064b25）、
        接入 CI 与打包（fastapi / uvicorn / jsonschema / httpx2）；CI run 11 全绿（含 Python 3.10）。
        F2.2 除裁决队列、episode 列表两个游标接口（归 W5）外验收全部满足，暂保持 not_completed
      · 契约 1.2（bc6b2e644）：C5 补 stopped/failed → succeeded/completed_with_errors（只对任务，resume 子任务结束后重算终态）、
        Subtask.pause_reason、set_subtask_result_rev、subtasks_in_states；C4 1.2.0：写请求一律带 JSON Content-Type、
        所有写接口接受 Idempotency-Key、method_not_allowed、TaskVlm、密钥删除后名称为 null、SSE 带 subtask_id/reason、
        日志顺序与 subtask 筛选、call_kind 与 stage id 放开
      · 在跑：W3 后半、W10 前端、W4 续作（仓储与只读接口跟上 1.1/1.2）、W8 密钥与资源管理（新派，从 bc6b2e644 起）
      · 合并 W8（bbc6262bb、5f9bc80a0）：daemon/secrets/（AES-GCM 加密存储与可续做的主密钥轮换、访问密钥与模型服务、
        写探针与预签名、开始前三项检查库、CLI 子进程环境）+ 三组路由；140 条测试，含「埋下密钥后在响应、日志、审计、
        数据库文件里搜不到」的验收用例；requirements 加 cryptography、CI 加 tests/secrets、console script curator-rotate-master-key。
        F2.6 保持 not_completed：「拿验证失败的密钥开始任务会被拦」要等 W5 调用检查库。W8 的 10 处契约缺口记入 SUMMARY §七（1.3 待改）；
        设计 08 跟进（轮换步骤、拉到的模型不逐个试调、子进程环境只放回 TOS_ENDPOINT）
      · 18:40 前再次因会话额度中断（W4 续作、W3 后半、W10），18:43 用原上下文恢复
      · 合并 W3 后半（b3939f0d1）：其余原子命令（autolabel、check、aggregate、export、report、adjudicate-apply）建在 v1 流水线上，
        B 类编排按需改、A 类未动；合成数据上冻结点 v1 对 v2 逐位对账 all-strict 通过（调用图 hits=121、misses=0），进 CI。
        随后修掉「恢复为可用的复议条目被放进 held」「裁决后又跑一次去重」两个问题（bf68f5208）
      · 合并 W4 续作（2332d26cc）：数据集登记与浏览、概览、仓储与只读接口跟上契约 1.1/1.2
      · 契约 1.3（075fcce9a）：Decision / DatasetDetail 改成共享字段 + unevaluatedProperties，终于能通过校验；19 个写接口补
        Idempotency-Key；PrecheckDetails、探针错误码、删除被已结束任务引用的密钥需确认、签名前缀、会话令牌、Subtask.pause_reason；
        C5 并入概览等六个查询和两个引用计数
      · 合并 W10 前端（53a1cf3e6、126938cbd）：八页 React（Arco），对着按契约校验的 mock 开发，181 条测试，接入 CI（Node 20/22）；
        Daemon + 前端在 /curation 下对真接口实测：概览、密钥、任务、数据集四页正常，只有 datasets/browse 404（归 W5a）
      · W3 核对 v1 找出三处差异，需求方拍板（评审记录 §12）：D39 改标重判默认用 v1 的两层（打分 + 逐机位复核），页面说明并可选
        首轮完整流程；D40 源文件缺失的 episode 照 v1 剔除，报告列出；D41 技能画像整体失败维持全部待补跑、等重试。
        未被否决的默认：review 种类照 v1；重复项又被人判成功仍按重复项拒绝；--vlm-reasoning-effort 给了才发
      · 契约 1.4：C4 1.4.0 执行裁决可选请求体 relabel_rerun（v1 / full）并记进子任务 scope、Summary.skipped；C2 共用的
        skipped_episodes（源文件清单、check 输出、report 完整性）、decisions / adjudicate-apply 的 relabel_rerun、final-list
        的 review 口径；设计 00–03、06、07、10 与账本跟进；契约 74、Daemon 312、v2 全部 1194 条、前端 181 条测试通过
      · 在跑：W5a 编排与执行器、W5b 读结果的接口（worktree 均从 126938cbd 起）
      · 合并 W10 续作（c1d1cbdef）：执行裁决确认框说明改标重判口径并可选完整流程、时间线标明口径、报告列出缺源跳过的条目；
        前端 189 条测试
      · 需求方答复三条默认做法：D42 已被拒绝的条目不再问成败，去重的拒绝可在「被拒复议」里恢复（推翻去重的结论）；
        D43 复核种类由注册表声明、接口留好；思考参数只在设置了才发送（维持）
      · 契约 1.5（8001712db）：C1 1.2 复核种类目录 + 每个模块的 review_lines / appealable；C4 1.5.0 裁决 line / decision
        改开放字符串、Daemon 按目录校验、duplicate_of、复议覆盖可复议模块、计数口径写明；C2 review 种类开放、带 line 与
        duplicate_of，decisions.json 已知三种仍钉住，报告小节可带 appealable；设计 00–02、05–07、10 与评审记录跟进；
        飞书副本已同步；契约 75、v2 后端 1195、前端 189 条测试通过
      · 新开 W11 镜像与 Helm Chart（worktree 从 98cd72780 起）；W3、W5b、W10 已收到 1.5 的改动
      · 需求账本的 D39–D43 修订说明已写（D42–D43 那段等需求方关掉编辑器后补上）
      · 23:40 前五个 agent（W3、W5a、W5b、W10、W11）因会话额度中断；额度重置后逐个用原上下文恢复
      · CI 自 #16 起一直是红的、没人发现：tests job 在第一个失败的步骤就停了，后面的 Daemon、密钥、导出、对账
        都没在 CI 上跑过；改成各组互不遮挡（8968b2e9b）。Linux 上剩三条失败，根因相同：对账工具的假模型按含 JPEG 字节的
        请求哈希作答、测试按哈希挑录制条目，macOS 与 Linux 的编解码库不同 → 交 W3 在源头改成与平台无关
      · 合并 W5b（121f36d2c）：报告、明细表切片、单条下钻、性能剖析、裁决队列与记录裁决，按 C4 1.5；62 条测试
      · 合并 W10（3b319df9f）：复核种类取自注册表目录、被拒复议含去重、报告「可复议（N）」；前端 193 条测试
      · 合并 W11（a0e3ee7f4）：多阶段镜像（前端构建 + Daemon，非 root）、Helm Chart（单副本 StatefulSet + 块存储）、
        部署检查 116 条、运维手册 deploy/README.md；本机无 docker，镜像构建看 CI，集群上的三条验收待上集群；
        修了一条会被本机 .DS_Store 弄红的构建上下文检查；CI 加读结果与部署两组测试、镜像里起 Daemon 与命令行（f8be9ba21）
      · 契约 1.5.1（8194f558e）：1.5 的严格校验挡掉了 v1「改标后顺手给成败结论（选填）」，由注册表 1.3 的 follow_ups 补回；
        另写明 status 筛选、采纳建议不带 new_label、单条视图的 EpisodeNote、读结果的 404 原因
      · 合并 W5b 续作（375a289e3，follow-up 校验与作废、Queue.executable）、W10 续作（ef28687b7，follow_up_of）、
        W3（db878deeb：D39 v1 两层重判且与 v1 rejudge 逐位对账、D40 缺源剔除、D42/D43 复核种类、思考强度参数、
        假模型改为与平台无关）、W5a（137b5d709：任务编排与 CLI 执行器，105 条测试覆盖 F2.3 全部验收，F2.6 开始前检查、
        F2.7 D37 流程、数据集 episode 列表）；C4 1.5.2（follow_up_of）；v2 全部测试、v1 1407、对账 68、A 类 57/0 通过
      · 需求方授权上集群联调（2026-09-22）：kubectl 部署 StatefulSet；APIG 复用 physicalai-apig、新服务 curation-v2；
        Basic Auth 沿用 v1 的 Secret curation-ui-auth（Chart 加 auth.usernameKey，不复制密钥）；主密钥 Secret 已建
      · 镜像：需求方建了火山 CP 流水线 build-data_curation-v2（推送 iaas-us-cn-beijing.cr.volces.com/physicalai/robot_curator:<提交号>）。
        推送触发没生效（代码源 Webhook 没接上，要在 GitHub 仓库或 CP 侧补），先在控制台手动运行；
        修了三处：ContextPath 还是 v1 的 robot-curation → 改为仓库根「.」；火山内网 PyPI 缺 uvicorn 0.53.0、
        cryptography 50.0.1 → deploy/pip-extra-index.txt 列出的包连同依赖从阿里云装，其余照旧走火山源（整体加
        --extra-index-url 会让大半包走阿里云、只有 ~300 kB/s）；构建参数不能以 PIP_ 开头（pip 会当成自己的选项）。
        第 6 次构建成功（69a890428，2 分 41 秒）
      · 集群部署（2026-09-22）：helm install curator-v2 到命名空间 curation（StatefulSet 1 副本、ebs-ssd 100Gi、
        Service、ConfigMap、physicalai-apig 的 Ingress curation-v2.apig.local /curation）；Pod 就绪、零重启；
        集群内实测：/healthz /readyz 200，不带账号 401、v1 的账号 200，注册表 8 模块 + 复核目录，控制台 200；
        公共数据集（ai-infra）浏览 7 个；登记集群内 vLLM（a30-8b，Cosmos-Reason2-8B）连通、模型列出；
        公共数据集 aloha_sim_transfer_cube_human 预检 1.2 秒通过（LeRobot v3、50 条、8 模块全部可用）
      · APIG 没接上：physicalai-apig 实例的 watchNamespaces 只有 dataverse/default/rlinf/autumn，不含 curation，
        Ingress 没被接管（ADDRESS 为空）；追加 curation 要改共享的 apiginstances 资源，被权限系统拦下，等需求方决定
      · CI：失败的 pytest 步骤把输出尾部写成公开可见的错误注解；据此查到任务编排那一步的退出码 2 是 CI 没装 httpx
        （恢复测试用它连真起的 Daemon），已补进 requirements-dev（3781e6ca8）；同分支新推送取消在跑的旧运行

      · CI 自 #16 以来第一次全绿（41a7ac70d，19 分 42 秒，所有测试组都跑）；按验收把 F2.1、F2.2、F2.3、F2.4、F2.6、F2.7
        置为 completed（20 项中 14 项完成）
      · APIG 接通（2026-09-22）：需求方把 curation 加进 physicalai-apig 实例的监听命名空间；Ingress 后端引用服务端口
        必须用端口号（用名字时 APIG 控制器报 MissingParameter.UpstreamSpec.K8SService.Port），Chart 已改（6ba597e6f）；
        网关上 https://sls9lfu8l6u30taht8cc7.apigateway-cn-beijing.volceapi.com/curation/ 已通（401 即 Basic Auth 生效，账号同 v1）
      · 需求方试用后提了三处界面改动（账本修订记录 2026-09-22）：VLM 模型列表分页 10 个一屏 + 模糊搜索 + 「设为默认」；
        「开始」等操作点下去立刻切状态；各表格列宽留余量。W10 在做前端，后端已先行：
        契约 C4 1.6.0（VlmModel.is_default 必填、VlmModelPatch.is_default）、C5 set_default_vlm_model（清旧设新一把事务，
        每位用户跨后端至多一个默认），迁移第 3 步加列与部分索引；删模型或删后端默认即消失，刷新模型列表不清默认（86217fd2b、535c7817e）
      · README 补「跑通一次完整质检（真数据）」八步（F4.3 的验收路径，b01974471）
      · CI：任务编排的并发策略测试在 Linux 上偶发红（客户端读到答复就发下一条，处理线程还没退出 → 假模型数到 2 个在途），
        改成答复发出前先把这条计数出去（5e611e465）
      · 合并 W10 的三项界面改动（1e45d1ce7）：模型列表每屏 10 个 + 模糊搜索（字母按序出现即可，d20lite 命中
        doubao-seed-2-0-lite-260215）+ 设为默认（跨后端唯一，新建任务预选并标「（默认）」）；开始 / 暂停 / 停止 /
        继续 / 重试 / 执行裁决 / 重新导出 点下去立刻写进缓存、不等轮询；表头不折行、各表列宽固定、数据集的最近一次
        质检把状态放到第二行。前端 205 条测试、lint、类型、构建全过；本机起 vite 逐项看过（.claude/launch.json 加了
        curator-frontend-dev）
      · 宽表的取舍：1280 宽的窗口里任务列表与数据集表改为横向滚动（操作列钉在右侧），不再挤压列宽；
        要在 1280 下看全所有列，得砍掉一两列 —— 留给需求方定
      · 镜像：CP 流水线仍然不会被推送自动触发，第 7 次构建在控制台手动发起（13641d566）

  - 2026-09-22 晚 · EEF–视频一致性（阶段 5，DEMO 模块）立项：
      · 设计 docs/design/12-eef-video-consistency.md 评审通过（§0.4 十项决策、§0.5 产品形态、§0.6 开工前六点均已确认）；
        格式规范、迁移规则与五份 Schema 在 docs/design/12-eef/，F5.1 冻结时迁入 docs/contracts/eef/ 并刷新 lock
      · 数据就绪（本机 ~/ws/ws_general/galbot/）：dataset2 = LeRobot v3.0（eef_ds2_lr3/）+ trajectory.json（七条、两路相机，
        参考校验器通过，最大重投影差 0.013 px）+ observations_seed/（P-A 种子，synthetic_fixture）+ corruptions.json（真值）；
        dataset1 = v2.1 十一条（单相机、十种轻重两档故障）+ trajectory.json + ground_truth/
      · 实施由另一位 agent 按 12 篇「开工指引」在 feat/curator-v2 上直接做：第一刀 F5.1–F5.4 纯 CPU，不接 VLM，不做上传控件

  - 2026-09-23 · EEF–视频一致性 DEMO 第一刀（F5.1–F5.4，本会话在 feat/curator-v2 上直接做）：
      · F5.1 契约与读取 completed：格式与五份 Schema 迁入 docs/contracts/eef/ 并入锁（示例 examples/eef-*.json）；
        backend/curation/extensions/eef_consistency/（contracts、load、geometry、timeline、capability、motion 的 L0、
        adapters 的 unified_sample 与 world_policy、__main__ validate）；dataset2 七条（2009 帧、28126 点、最大差 0.013 px）
        与 dataset1 十一条（3344 帧、13376 点）读通，形态 B 全部 available；真值键拒收并给路径；几何与 OpenCV 针孔 /
        Brown / 鱼眼交叉一致；客户 droid / LVP 各 10 个收录且 unsupported；tests/eef 65 条（CI 加一步，DEMO 数据缺失时跳过）
      · 口径（12 篇附录 C.1）：投影自洽超差是警告 input_inconsistent、两份都保留；L0 折叠重采样重复帧、用机器人时钟；
        基准高频滚动 RMS 最大 1.09 mm，§12 的 5 mm 起点会漏掉 dataset1 ep5，demo 阈值按基准噪声底定
      · 从仓库根跑 v1 单测有一条起子进程的用例找不到包（CI 在 backend/ 下跑，没问题）；CLAUDE.md 与 12 篇的命令已改

Main gaps:
  - F1.1 基线暂缓（需求方 2026-09-21 决定），F4.2 黄金对账之前按 tools/parity/README.md 在 Pod 里补
  - APIG：需要在 physicalai-apig 实例的监听命名空间里加 curation（需求方或实例维护者）
  - 完整跑一次质检要一把能写交付目录的 TOS 访问密钥（在 v2 的「密钥与资源」页填）；之后验 F4.1 的「升级时暂停、新 Pod 续跑」
  - 火山 CP 流水线的推送触发没生效（GitHub 仓库的 Webhook），目前手动运行
  - F3.2 待需求方的文案统一修改；F3.3 待集群上用真 TOS 数据走一遍（视频预签名与重签）；F4.1 待验升级续跑；
    F4.3 待按 README 完整跑一次质检

Next steps:
  0. EEF–视频一致性 DEMO 第一刀：F5.2 独立观测（P-A 跟踪；dataset1 的种子要按真值补）→ F5.3 指标与诊断、§13.3 矩阵 → F5.4 接入 v2；
     §3.3 mapping.yaml 导出工具留作后续
  1. APIG 接通（physicalai-apig 监听 curation）、TOS 密钥就位后：在集群里完整跑一次质检（含人工裁决与导出），再验升级续跑
  2. 按 README 走查一次完整质检，F3.3、F4.1、F4.3 按验收置位；请需求方统一过一遍界面文案（F3.2）
  3. 真实数据的两个基线在 F4.2 黄金对账之前补
