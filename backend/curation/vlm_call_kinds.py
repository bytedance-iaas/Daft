"""五类 VLM 调用的单一事实源:显示名 / 流程顺序 / 一句话解释。

为什么要有这个模块(2026-08-15):同一类调用在界面叫「任务判定探针」、在客户
报告里叫「渐变问询(VOC)」,两套名字都不准("探针"与 probe_endpoint 的**探活**
一词两义;"渐变问询"是中英夹杂的内部黑话),解释还写错了两处事实(复核被说成
"只对没通过一审的跑",实际全员运行;打分被说成"逐帧问",实际抽样后按时刻多视角
联合问)。名字与解释从此只在本处定义,ui/manifest.py 与 export/report.py 都从
这里 import —— 改一处,两边同变。

⚠️ 红线:
- 左列的埋点标签(probe/endstate/...)是 details/vlm_latency.csv 的数据契约,
  老交付还要读得回来,**一个字不许改**;要改的只有右边的显示名与解释。
- 本模块必须保持**零依赖**(不 import 管道/UI/三方库):UI 有"不 import 管道
  代码"的红线(先例见 dataset_level/decisions.py 的纯 IO 层),这里同理。
"""
from __future__ import annotations

#: 流程顺序(2026-08-15 用户定,方案 B):条形图与解释都按漏斗里发生的先后排,
#: 不再按耗时从长到短排 —— 谁最费时靠条长看,顺序留给"这套流程怎么走"。
CALL_KIND_ORDER = ["probe", "endstate", "arbitration", "caption", "llm"]

#: 埋点标签 → 显示名。键只作字典键(CSV 契约),界面/报告上只出现值。
#: 「任务完成度打分」不叫"探针":代码里 probe_endpoint 是探活,一词两义;
#: 「逐机位复核」读取各机位连续视频，逐机位各投一票。
CALL_KIND_LABELS = {
    "probe": "任务完成度打分",
    "endstate": "逐机位复核",
    "arbitration": "取证仲裁",
    "caption": "技能打标",
    "llm": "技能归纳",
}

#: 每类调用一句话:干什么 + 跑多少次。三处事实有测试钉死,不许改回错误说法:
#: ① 打分是多机位连续视频联合判断；② 复核**全员运行**并有
#: 否决权(不是"只对没通过一审的跑");③ 只有取证仲裁是**拿不准(弃权)才跑**
#: (task_success.py:仅 passed is None 进链)。
CALL_KIND_NOTES = {
    "probe": ("联合读取一条数据的多机位连续视频,判断任务是否完成,"
              "给出完成度、理由和证据时间段;每条一次主判请求。"),
    "endstate": ("对**每条**数据都跑的独立复核:各机位分别读取完整视频片段,"
                 "给出完成、失败或证据不足及时间段证据;主判与复核冲突时转人工。"),
    # ⚠️ 收尾的 `**` 前面**不许是标点**:CommonMark 里"前有标点、后接文字"的
    # `**` 不算合法收尾定界符,整段星号会原样印在页面上(2026-08-15 实见:
    # `**拿不准(弃权)**` 在界面上就是带星号的字面量,而同段的 `**每条**` 正常
    # 加粗 —— 差别只在收尾前一个字符是汉字还是右括号)。把括号挪到加粗之外。
    "arbitration": ("只对打分与复核后**拿不准**(弃权)的数据再次核对连续视频:"
                    "复查动作过程与物体变化,引用时间段证据,判废需两路相机复核印证。"),
    "caption": ("读取每条数据的多机位连续视频,写一句「它在做什么」,"
                "优先用于技能归类,并用于无标注数据补标;"
                "每条至少一次,存疑标注会加问几轮。"),
    "llm": ("通读全部打标结果,归纳出这份数据的两级技能体系并复核标注质量;"
            "整批只跑几次,但每次输入很长。"),
}
