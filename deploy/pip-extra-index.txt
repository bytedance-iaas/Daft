# Pins the Volcano PyPI mirror lacks, installed without dependencies from SUPPLEMENT_PYPI_URL
# (Aliyun by default) before everything else comes from PIP_INDEX_URL. Keep each line identical
# to backend/requirements.txt (a test checks); drop a line once the Volcano mirror has it.
uvicorn==0.53.0
