# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
dist/
build/

# Local secrets and data
.env
.env.*
data/

# Node
**/node_modules/

# Editors / OS
.idea/
.vscode/
**.DS_Store

# Claude Code local state (worktrees of background agents)
.claude/worktrees/

# The requirement ledger and the progress notes stay local, never pushed (requester, 2026-09-24)
/feature_list.md
/claude-progress.txt

# v1 intermediates (from robot-curation/.gitignore)
.pip_install.log
smoke_out/
spikes/smoke_out/
# large data files never go into git
*.parquet
*.mp4

# EEF evaluator run artifacts (evidence images, curves); the summary JSONs next to them are kept
tools/eef_eval/reports/run/
