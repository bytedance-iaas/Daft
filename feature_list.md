# Curator v2 需求账本

> 需求来源：《Physical AI Kit - Curator 设计文档Prompt》+ 两轮澄清（冻结决策见
> `docs/design/00-overview.md` §7）。
> **纪律**：feature 的描述、验收标准一经写入不得改写；只允许更新 `status` 字段。
> `status` 取值：`not_completed` / `completed`。

## 阶段 0：设计

### F0.1 需求澄清
- 描述：读需求文档，梳理歧义点，与需求方确认到可开工
- 验收：所有阻塞性问题有明确答复，决策记入设计文档 §7
- status: **completed**

### F0.2 工程设计文档
- 描述：输出 12 篇设计文档，覆盖架构/数据模型/CLI/REST/并发/模块/交付/前端/密钥/部署/对账/工作包
- 验收：文档可支撑多 agent 并行开工，接口契约明确到可编码
- status: **completed**

### F0.3 设计文档评审与完善
- 描述：对照需求文档、v1 代码、v1 使用文档与方舟 API 文档评审 12 篇设计文档；澄清遗留问题；逐项修订并同步飞书评审副本
- 验收：①`docs/design/review-2026-09-20.md` 中每一项都有结论（已修订 / 不改及理由）；②需求方拍板的问题记入 `00-overview.md` §7 决策表；③仓库 Markdown 与飞书副本内容一致
- status: **completed**

### F0.4 第二、三轮评审意见整合
- 描述：整合另一位评审者的 14 条问答（D24–D32）与 `review-catalog.md` 的 F01–F28，逐条落到 12 篇设计文档并同步飞书副本
- 验收：①评审记录 §6、§7 两张对照表逐条有去向；②新增决策记入 `00-overview.md` §7，新增「需求覆盖表」；③仓库 Markdown 与飞书副本内容一致
- status: **completed**

## 阶段 1：地基（串行，不可跳过）

### F1.1 对账工具与黄金基线
- 描述：实现 `curation-parity compare`；用 v1 代码对 `umi_640_notask` 跑一遍完整质检，存档为黄金基线
- 验收：①工具对 v1 自己的两次跑批比对结果为「完全一致」；②基线产物含八模块完整结果，已归档
- status: not_completed

### F1.2 Baseline 清理与目录重组
- 描述：删除上游 daft 源码，按设计文档 §6 重组目录，删除 Gradio UI 与内嵌终端，清理依赖
- 验收：①CLI 可运行；②搬运的单元测试全绿；③镜像可构建；④每步独立 commit 可回滚
- 前置：需求方确认删除上游代码
- status: **completed**

### F1.3 契约冻结
- 描述：定稿 C1 模块注册表、C2 CLI JSON schema、C3 进度协议、C4 REST OpenAPI、C5 Repository 接口
- 验收：三份 schema 为真实文件且被契约测试消费；CI 在契约漂移时变红
- status: **completed**

## 阶段 2：后端

### F2.1 CLI 原子命令
- 描述：preflight / plan / decode / check / aggregate / export / report / adjudicate-apply 八条命令
- 验收：①每条命令 `--json` 通过 schema 校验；②全链路跑通 8 条 episode；③默认不并发不重试，并发与重试为显式参数；④凭证只经环境变量，不入 argv
- status: **completed**

### F2.2 Daemon 骨架
- 描述：FastAPI 应用、SQLite Repository、鉴权中间件、SSE、静态资源托管、健康探针
- 验收：①探针豁免鉴权；②SSE 支持 Last-Event-ID 重放；③主密钥缺失时拒绝启动；④游标分页无重复无遗漏
- status: **completed**

### F2.3 任务编排
- 描述：任务/子任务状态机、worker 池、暂停恢复、停止、优雅停机
- 验收：①暂停恢复后结果与不暂停一致；②停止后无孤儿子进程；③进程重启后任务状态可恢复；④非法状态迁移返回 409
- status: **completed**

### F2.4 planner 与 VLM 请求合并
- 描述：执行计划生成、CPU/VLM 两档并发、同 episode 多模块合并、usage 采集
- 验收：①合并与单发模式判决一致率 ≥98%，差异条目人工确认无系统性偏差；②token 摊派总和等于实际用量；③合并可通过配置一键关闭
- status: **completed**

### F2.5 增量重新导出
- 描述：export manifest、diff 算法、LeRobot v2/v3 两条增量路径
- 验收：①剔除中间一条 episode 后重新导出，产物可被官方 lerobot loader 无警告加载；②未受影响的视频文件字节不变；③视频先写本地临时文件再整文件拷贝（FSX 随机写限制）
- status: **completed**

### F2.6 密钥与资源管理
- 描述：AES-GCM 加密存储、连通性校验、方舟拉模型列表、自定义端点
- 验收：①任何 API 响应与日志都不含密钥本体；②TOS 凭证校验含写探针；③删除被运行中任务引用的凭证返回 409
- status: **completed**

### F2.7 预检
- 描述：格式识别、模块可用性三态、机器人型号追问
- 验收：①非 LeRobot v2/v3 全模块标灰并给出原因；②规格库不支持的型号 → 整项跳过且任务不失败；③读不到型号 → needs_input；④未勾选该模块则不追问
- status: **completed**

## 阶段 3：前端

### F3.1 静态 HTML 预览
- 描述：先出任务列表 + 新建任务两页定风格，确认后补齐详情/报告/裁决/密钥四页
- 验收：需求方确认风格
- status: **completed**

### F3.2 前端实现
- 描述：React + Arco Design 六个页面
- 验收：①深链参数行为与 v1 逐项一致；②SSE 不可用时自动降级 5s 轮询；③所有表单必填项红 `*` 且校验；④前端零业务状态，刷新不丢；⑤模块清单完全数据驱动
- status: not_completed

### F3.3 人工裁决页
- 描述：统一裁决入口，按来源模块分组；三条裁决线语义与 v1 一致
- 验收：①「整条弃用」压过成败裁决；②复议只对归因任务成败判定的拒绝开放；③「拿不准」条目保留在队列；④视频走预签名 URL，过期自动重签
- status: not_completed

## 阶段 4：交付

### F4.1 镜像与 Helm Chart
- 描述：多阶段 Dockerfile、Chart（单副本、PVC、Secret、探针、优雅停机）
- 验收：①VKE 上 helm install 一次成功；②滚动升级时运行中任务置 paused 且可恢复；③帧缓存卷非 FSX 挂载
- status: **completed**

### F4.2 黄金对账验收
- 描述：对 `umi_640_notask` 全量执行对账
- 验收：①CPU 五项（时间戳/运动学/运动质量/视觉质量/视频-动作同步）与 dedup 逐位一致；②task_success 与 skill_profile 判决差异 <2% 且逐条人工确认
- status: not_completed

### F4.3 README 与手动验证步骤
- 描述：持续维护 README，确保人工验证步骤始终可执行
- 验收：按 README 步骤可完成一次完整质检并看到报告
- status: not_completed

## 阶段 5：EEF–视频一致性（DEMO 模块，2026-09-22 立项）

> 设计：`docs/design/12-eef-video-consistency.md`（§0.4 决策、§0.5 产品形态、§0.6 开工前六点已由需求方确认）。
> 数据：`~/ws/ws_general/galbot/dataset2`（`trajectory.json` 上传件、`observations_seed/` 种子、`corruptions.json` 真值）与 `dataset1`。

### F5.1 契约与读取
- 描述：`backend/curation/extensions/eef_consistency/` 的 contracts / load / geometry / timeline / adapters；读取并校验 `trajectory.json`（`trajectory-bundle/1.0` 容器 + `eef-video/1.0.0` 三段）；逐分项能力预检；几何投影与「提供 vs 重算」自洽；L0 数值轨迹（速度、旋转增量、尖峰、高频能量）；格式与 Schema 从 `docs/design/12-eef/` 冻结进 `docs/contracts/eef/` 并刷新 lock
- 验收：①dataset2 七条与 dataset1 十一条的 `trajectory.json` 读通，能力表正确（形态 B 全部 available）；②重算投影与提供投影之差在源量化精度内（< 0.05 px）；③真值键出现即拒绝；④几何单测与 OpenCV `projectPoints` 交叉一致；⑤契约测试与 lock 检查通过
- status: **completed**

### F5.2 独立视觉观测
- 描述：ObservationProvider 协议与观测文件（observation Schema）；P-A provider：种子（`observations_seed/`）+ 多尺度 LK 跟踪 + 前后向校验 + 周期重定位 + 遮挡弃权；投影扰动的独立性实验
- 验收：①provider 输入不含投影、真值、注入参数；②投影平移 30 px 时观测不变；③失跟不用插值冒充观测；④dataset2 两路相机的可见帧定位覆盖与弃权率有报告
- status: **completed**

### F5.3 CPU 指标、诊断与离线报告
- 描述：位置、方向、局部 lag、数值轨迹、画面共同运动五项指标；迟滞分段；诊断假设（PnP 外参修正、lag、恒定朝向、轴向偏移、漂移、抖动来源）；曲线与证据；`demo` 阈值 profile（标 uncalibrated）；离线评估器读真值算命中
- 验收：①12 篇 §13.3 受控异常矩阵逐行符合（dataset1 + dataset2，共 18 条）；②基准两条各分项 ok 且覆盖率有报告；③lag 符号按 `u_visual(t) ≈ u_declared(t + lag)`，dataset2 ep5 为 +0.333 s、dataset1 ep9/10 为 −0.200 / −0.533 s；④ep6 只在 ext1 报外参偏差，ext2 正常
- status: **completed**

### F5.4 接入 v2（advisory）
- 描述：注册表 1.4（`eef_video_consistency` frame 档、`eef_video_review` vlm 档、`eef_input` 能力、`input_scope`、`affects_dataset_verdict`）；预检逐分项能力；`check` 分派新 runner；parts 输出（result-record，`passed=None, score=None`，分项进 detail）；aggregate 调用边界过滤；报告默认表格与三张 TableSpec；`trajectory.json` 走 CLI 参数
- 验收：①关闭模块时黄金基线与接口快照逐位一致（parity）；②开启模块（含缺输入、异常、报错）旧 keep / drop / held 与交付清单一致；③报告里能看到逐相机分项、覆盖率与诊断；④被旧硬门拒绝的样本仍被本模块评估
- status: **completed**

### F5.5 上传控件与 Daemon 上传接口
- 描述：`param_schema` 文件型参数（`format: upload`）；Daemon 任务输入文件上传接口（存任务 inputs 目录，返回句柄与 hash）；前端新建任务第二屏的文件项；C4 修订
- 验收：①上传即校验，错误定位到样本 / 帧 / 字段；②文件 hash 进 input_hash，换文件重跑视为新输入；③openapi 更新并 `npm run gen:api`
- status: completed

### F5.6 VLM 复核
- 描述：均匀抽查 + 候选窗口；请求包（原图、裁剪、观测与投影异色叠加、短序列）；严格输出 Schema；预算、缓存、tape 录制回放；`eef_review` 调用种类；CPU 与 VLM 冲突转人工
- 验收：①固定 tape 下 malformed / timeout / 引用不存在帧 / 冲突分支全覆盖；②复核失败显示「未完成」，不影响旧判决；③VLM 不产生像素或厘米数值
- status: completed

### F5.7 验收与试用
- 描述：冻结阈值后在独立 episode 上报误报、漏报、定位与方向误差、覆盖与弃权率；性能记录；对指定数据集开 advisory 试用
- 验收：①独立 episode 分组验收报告；②每分钟视频的 CPU 耗时、峰值内存、VLM 请求数有记录；③回退 = 取消勾选，旧流程不受影响
- status: not_completed

## 阶段 6：第二轮界面反馈、mcap / lance 接入与 dataverse 替换（2026-09-23 立项）

> 来源：需求方 2026-09-23 的 24 条修改意见（逐条去向见文末「修订记录（2026-09-23 第二轮反馈）」），决策见 `docs/design/00-overview.md` §7 的 D44–D48。

### F6.1 导航、概览与任务页
- 描述：侧边栏顺序与「帮助 · 使用文档」、面包屑「数据质检平台」链到概览（第 5、7 条）；概览只留「运行情况」「Token 消耗」两张并排卡片，时间范围可选，运行中任务的列表超出时分页（第 8 条）；有结果的任务常驻「人工裁决」入口（第 3 条）；子任务（重试等）运行时任务显示「运行中」（第 4 条）；任务详情去掉重复的「查看详细报告」、「分档进度」与「Token 消耗」并排且 Token 带明细、卡片标题旁的说明文字去掉、执行时间线改横向、「终判 + 报告」显示为「报告生成」、「导出 + 交付核验」显示为「交付」（第 10、11、13、21 条）；任务列表进度两行、不写剩余时间（第 20 条）；删除任务可同时清理交付产物（第 19 条）
- 验收：①侧边栏依次为 概览 / 质检任务 / 数据集 / 密钥与资源，另有「帮助」分组下的「使用文档」；面包屑首项「数据质检平台」可点、回到概览 ②概览只有「运行情况」与「Token 消耗」两张等高卡片；时间范围可选近 7 天 / 近 1 月 / 近 3 月 / 近 1 年；运行情况里的任务列表放不下时分页 ③有结果的任务在详情页头、报告页头、列表「更多」里都能进入人工裁决，只剩可复议条目时也能进 ④点「重试」后列表与详情都显示「运行中」，子任务结束后回到重算后的终态且列表自动刷新；子任务的实时事件不覆盖任务自身的状态 ⑤详情页没有「查看详细报告」；分档进度与 Token 卡片并排等高，Token 明细可按模块 / 子任务切换并有合计行；卡片标题旁没有说明文字；执行时间线横向排列 ⑥分档进度、任务列表、概览里「终判」「报告」合并显示为「报告生成」，「导出」「交付核验」合并显示为「交付」 ⑦任务列表的进度列两行（当前档、总进度），不显示剩余时间；分档进度不显示预计时间 ⑧删除确认框有一个默认不勾的「同时清理交付产物」，勾选后先清理再删除，清理失败则不删除
- status: not_completed

### F6.2 报告改版与 Episode 明细
- 描述：报告每个模块给统计指标与图表（不列具体 episode），小节可折叠（第 1 条）；报告新增「Episode 明细」页签，搜索下拉按 episode 编号模糊查询，按模块逐块展示该条的详细读数，取代底部明细表与右侧抽屉（第 2 条）；「数据包完整性」全中文、横向排版，去掉「小节和本次开启的 N 个模块一一对应」（第 18 条）；多路视频同步播放：不自动播放，缓冲不足时全部暂停（第 14 条）；后端补模块统计、任务 episode 列表与同步曲线接口，契约随之修订
- 验收：①八个模块与两个 EEF 模块的小节各有统计指标与至少一张图（没有数据时说明原因），不出现逐条列表或 JSON 原文；每节可折叠，另有全部折叠 / 展开 ②报告页签依次为 报告 / Episode 明细 / 性能剖析，底部不再有明细表；输入「12」或「ep12」能找到 ep 12，可按判决筛选、上一条 / 下一条；每个模块一块，读数全中文 ③点报告里任一「ep N」进入该页签并定位到这一条，地址可以分享 ④数据包完整性没有英文键名与原始 JSON，横向排版 ⑤同时播放：点之前不播放，切换 episode 不自动播放；起播前各路都已缓冲就绪；任一路缓冲不足时全部暂停，恢复后一起继续；暂停或拖动其中一路，其它路跟随 ⑥新增统计不改变任何判决（合成数据上 v1 对 v2 对账仍逐位一致）；契约测试与锁检查通过
- status: not_completed

### F6.3 表单、数据集与密钥页
- 描述：VLM 后端模型列表的查询与添加（第 9 条）；新建任务的文案（第 12、15、16 条）；添加数据集按钮（第 17 条）；数据集列表的操作改为「可视化 / 新建任务 / 删除」，可视化跳到同域 ReRun 并带上 TOS 地址与地域（第 22 条）
- 验收：①拉到模型列表时，表格上方是只做查询的模糊筛选框；拉不到时是「输入模型 ID + 添加」；「设为默认」与「移除」挨在一起 ②「创建并开始」不再出现「正在做开始前检查…」；预设写「快速质检」；「高级设置」旁没有说明文字；Episode 选择写「全部」；第二屏没有「无需额外设置：…」 ③添加数据集提交时按钮文案不变 ④数据集列表每行的操作为「可视化」「新建任务」「删除」（红色）；「可视化」在新标签页打开 `<同域>/?url=<TOS 地址?region=…>`，ReRun 直接加载这个数据集；本地来源的数据集按钮置灰并说明原因
- status: not_completed

### F6.4 ID 风格统一
- 描述：新建记录的 ID 改为「前缀-9 位小写字母」（task、sub、ds、pf、cred、vb、vm、upl），已有记录保留原 ID（第 6 条，D45）
- 验收：①新建的任务、子任务、数据集、预检缓存、访问密钥、VLM 后端与模型、上传文件的 ID 都符合 `^[a-z]+-[a-z]{9}$` ②旧格式 ID 的记录照常读写、跳转、清理工作目录 ③ID 撞车时自动重新生成，不返回 500 ④契约与锁更新，测试通过
- status: not_completed

### F6.5 mcap / lance 格式接入
- 描述：把 `dev` 上 PR #155（mcap 与 lance 格式的质检）同步进 v2，冻结点前移到 `dev@eb637ba40`（D44），A 类清单与对账随之更新；接入 v2 全链路：预检识别、数据集登记与浏览、质检、交付（mcap 原格式拷贝，lance 交 episodes_parquet）、报告与前端格式标签；支持 TOS 上的 mcap / lance（先拉到本地缓存再读）（第 23 条）
- 验收：①A 类文件与新冻结点逐个一致，合成数据上 v1 对 v2 对账全过 ②本地与 TOS 上的 mcap、lance 合成数据集都能登记、预检、跑完整质检并交付 ③LeRobot 数据集的结果与接入前逐位一致 ④补丁自带的 mcap / lance 测试在 v2 里全过；镜像带上新依赖
- status: not_completed

### F6.6 替换 dataverse 的 curation 组件
- 描述：在 galbot 命名空间下掉 dataverse 的 v1 curation（`galbot-dataverse-curation`），装上 curator-v2，挂在 `galbot-dataverse-apig` 同一域名的 `/curation`；数据盘与主密钥从 curation 命名空间迁过来，登录沿用 dataverse 的 htpasswd（第 24 条，D48）
- 验收：①dataverse 的其它组件（web viewer、catalog）不受影响 ②该域名下 `/curation` 打开 v2 控制台，用 dataverse 的账号登录；ReRun 的「质检」按钮跳过来能预填数据集；v2 的「可视化」能打开 ReRun ③迁移前的任务、数据集、密钥、模型都在 ④curation 命名空间的旧实例已下线
- status: not_completed

## 修订记录（2026-09-20 设计评审）

> 原有条款一字未改。下面是需求方在评审中拍板的决策对各 feature 的追加与替换说明，
> 与原文冲突处以本节为准。决策编号见 `docs/design/00-overview.md` §7。

- **F1.1 / F4.2（D19）追加**：黄金基线为两个数据集 —— `umi_640_notask` 与 `droid_lerobot` 前 50 条，两个都要过。
  对账比较的是规范化记录，v1 一侧由 `tools/parity/dump_v1.py` 导出；F1.1 验收①的「完全一致」指确定性六项，
  VLM 三项给出 v1 自身的波动基线。
- **F2.1（D18 / D22）替换**：原子命令为 preflight / plan / autolabel / check / aggregate / export / report /
  adjudicate-apply / verify 九条（`decode` 取消，新增 `autolabel` 与 `verify`），另加客户端命令 `curation task …`。
  `check` 可一次接受同一档内的多个模块。
- **F2.2（D21）替换**：验收④的「游标分页」改为：任务列表页码分页且 `total` 正确；裁决队列、日志、episode 列表用游标。
- **F2.3（D20）追加**：状态机含「待启动」与系统暂停；`stopped` / `failed` 可继续运行；优雅停机超时不把任务置为失败。
- **F2.4（D23）替换**：现有两个 VLM 模块不参与合并、按 v1 调用图原样跑；验收①②由示例模块承担，
  另加「N=64 时推导出的闸门与 v1 出厂默认逐项相等」。
- **F2.7 追加**：没有任务标注不导致 VLM 模块标灰（由 autolabel 补）；未选 VLM 后端 → needs_input。
- **F3.2 追加**：在 `/curation` 挂载前缀下可用；任务列表页码分页。
- **F4.1 追加**：StatefulSet + 块存储数据卷；不再有帧缓存卷；升级后系统暂停的任务自动续跑。

## 修订记录（2026-09-20 第二、三轮评审）

> 同样只追加，不改原条款。

- **F2.1 追加**：原子命令为十条（增加 `snapshot`，固化源文件清单）；读源数据的命令接受 `--source-manifest`，对不上以退出码 6 结束；
  输入与输出用两组独立的凭证环境变量。
- **F2.3（D24–D26）追加**：执行出错的 episode 暂不交付、不进后面的档；补跑后按当前结果重算任务终态；
  新的结果版本完整生成后才切换；反复搞崩进程的 episode 点名跳过；模块级熔断。
- **F2.4（D31）追加**：调用方可设 CPU 并发、VLM 并行度、超时、重试的上限，实际取值取各层上限的交集；Token 记两本账，总量只算实际调用账。
- **F2.6（D30、D8）替换**：验收②③改为 —— 保存时只验身份；任务开始前输入读、输出写、VLM 最小调用三项必须通过；
  模型列表能拉就拉、拉不出手填，不维护内置清单。
- **F3.3（D32）追加**：人工裁决不跨任务。
- **F4.2 追加**：VLM 三项首选回放对账（请求 100% 命中、记录逐位一致），真模型统计比对为第二道；v3 格式本期不做对账（需求方确认的已知缺口）。

## 修订记录（2026-09-20 开工前核对）

> 同样只追加，不改原条款。决策见 `00-overview.md` §7 的 D33、D34，经过见评审记录 §9。

- **F1.1 追加**：以 `release_v1` 的 `45bdf9292` 为冻结点（D34）；基线在现网 v1 的 Pod 里生成；基线跑批零失败
  （有失败则「回放 + 补录」再跑）；v1 接回放后端再跑一遍，请求 100% 命中、全部规范化记录逐位一致；
  噪声底用 droid 前 50 条两遍、umi 前 64 条两遍，umi 640 条全量跑一遍作基线。
- **F1.2**：需求方已同意删除上游 daft 源码与不再需要的代码。基线生成期间可以推进 F1.2、F1.3；
  F2.1 起动 B 类代码之前，两个基线必须存档。
- **F2.1 / F2.3（D33）追加**：「执行出错」取宽口径 —— 判某条 episode 时任何一次模型调用重试用尽、或任一机位解码失败，
  该模块对它记 `error`、整条待补跑，不看 v1 的降级逻辑能否给出结论；补跑时该模块对它从头重跑。
- **F3.1**：需求方已确认第一批两页的风格。

## 范围外（本期明确不做）

- 新质检模块的算法实现；算法调优、阈值调整
- mcap / LanceDB 的 reader
- 火山 IAM 对接（只留 AuthProvider 抽象）
- 跨 episode 的 VLM 帧合并（只留 MergeStrategy 扩展点）
- VLM 并发度与合并粒度的调优（单独任务）
- 多副本、外置 RDS、请求层 LB
- 与火山控制台的像素级样式对齐

## 修订记录（2026-09-21 契约冻结核对）

> 同样只追加，不改原条款。决策见 `00-overview.md` §7 的 D35，经过见评审记录 §10。

- **F2.1 / F2.3（D35）追加**：已被正常判完的模块确定拒绝的条目（某个硬门确定失败，或所有已勾选的软分模块都给了分、
  加权分低于阈值），即使另有模块对它执行出错，也直接拒绝、不进 `held`，出错的模块照样记在该条目上；
  「重试」只补跑 `held` 里的条目；任务终态看有没有整体失败的模块和待补跑的条目。
- **F1.1 / F2.1 追加**：需求方决定暂不生成黄金基线（v1 在现网 Pod 上跑过、结果正常，旧系统有备份）。
  「F2.1 动 B 类代码之前两个基线必须存档」的卡点取消，改由合成数据集上冻结点 v1 与 v2 的逐位对账守住（进 CI）；
  真实数据的两个基线在 F4.2 黄金对账之前补。F1.1 在基线归档之前保持 not_completed。

## 修订记录（2026-09-21 静态稿评审）

> 同样只追加，不改原条款。决策见 `00-overview.md` §7 的 D36–D38，经过见评审记录 §11。

- **F3.1**：六页评审通过；文案问题等全部页面实现之后一次性修改。
- **F3.2 追加（D36、D38）**：页面增加「概览」「数据集」（含数据集详情）；新建任务分两屏（第一屏基本且必填，
  第二屏按已开启模块给出各自的额外输入）；任务列表的质检模块列只放摘要，另可按模块筛选；
  状态「部分错误」改叫「错误」；人工裁决以 episode 为索引、卡片写明来源模块；复议按钮「恢复为可用 / 维持拒绝」；
  从没导出过的叫「导出」。
- **F2.2 / F2.7 追加（D36、D37）**：数据集登记（添加时预检，记下 meta 指纹与全量文件清单指纹）；
  开始质检任务时核对指纹，不一致时返回变化，经用户确认后重新预检，相容则继续开始，不相容则回到编辑页。

## 修订记录（2026-09-21 W3 核对 v1 差异）

> 同样只追加，不改原条款。决策见 `00-overview.md` §7 的 D39–D41，经过见评审记录 §12。

- **F2.1 追加（D39、D40）**：改标重判默认照 v1，只跑多视角打分和逐机位复核两层，可选首轮的完整流程，
  选择随每条改标记在批次目录里、重试沿用；源文件缺失的 episode 照 v1 剔除，不质检、不进任何清单、不计入总数，
  报告列出哪几条、各缺什么文件；review 的种类照 v1 的核对结果（`task_verdict` 只收任务成败判定的弃权，
  可复议的拒绝都进 review）；`--vlm-reasoning-effort` 给了才发。
- **F2.3 追加（D41）**：技能画像整个模块失败时，全部条目待补跑、一条都不交付，等「重试」成功。
- **F3.2 追加（D40）**：报告总览与数据包完整性一块列出缺源文件被跳过的条目。
- **F3.3 追加（D39）**：「执行裁决」的确认框说明改标重判的口径，并提供「按首轮的完整流程重判」的选项。

## 修订记录（2026-09-21 需求方答复默认做法）

> 同样只追加，不改原条款。决策见 `00-overview.md` §7 的 D42–D43，经过见评审记录 §12 末尾。

- **F1.3 追加（D43）**：模块注册表带复核种类目录，每个模块声明产生哪几种复核、它的拒绝可否复议；
  C2、C4 里复核种类与判断改为开放字符串，以后加一种复核不改契约（已随契约 1.5 完成）。
- **F2.1 追加（D42、D43）**：已被拒绝的条目（被去重或任何模块拒掉）不再有成败裁决项；去重的拒绝可以复议，
  恢复为可用即推翻去重的结论、回到通过，另有模块对它出错的，恢复后进待补跑；`adjudicate-apply` 遇到没有执行规则的
  复核种类报参数错误。
- **F2.2 追加（D43）**：提交的裁决按注册表的复核目录校验，种类不在目录里、或卡片上没有这个问题的，返回 400。
- **F3.3 追加（D42、D43）**：复议页签改叫「被拒复议」，列出任务成败判定与去重拒掉的条目，去重的卡片写明与哪条重复、
  并排放两条的视频；没有专用视图的复核种类按注册表目录通用渲染；复议不计入待裁。

## 修订记录（2026-09-22 需求方试用后的界面反馈）

> 同样只追加，不改原条款。

- **F3.2 追加**：VLM 后端的模型列表每屏 10 个分页，底部添加改成模糊搜索、列表随输入实时更新；
  模型可以「设为默认」，每位用户跨后端至多一个默认模型，新建任务默认选它，没有默认模型时仍让用户选
  （契约 C4 1.6.0 的 `VlmModel.is_default`、C5 的 `set_default_vlm_model`）。
- **F3.2 追加**：「开始」「暂停」等操作点下去立刻切到该操作对应的状态，不等下一次轮询。
- **F3.2 追加**：各表格列宽留余量，表头不折行；「数据集」的最近一次质检把状态放到第二行。

## 修订记录（2026-09-22 EEF–视频一致性立项）

- 新增阶段 5（F5.1–F5.7）：客户提出的「EEF 轨迹投影与视频一致性」需求，作为 DEMO 状态的第九个模块立项；设计见 `docs/design/12-eef-video-consistency.md`，需求方已确认 §0.4 十项决策与 §0.6 六点。
- 产品口径：不改 LeRobot 任何字段；用户在模块详细配置里上传约定格式的 `trajectory.json`；首版 advisory，不参与 keep / drop / held。
- 范围外：训练通用夹爪模型；人手 / 第一人称数据的 EEF 检查；移动相机的完整诊断；从视频反推轨迹。

## 修订记录（2026-09-23 第二轮反馈）

> 同样只追加，不改原条款。决策见 `00-overview.md` §7 的 D44–D48。

- 需求方 24 条意见的去向：第 3、4、5、7、8、10、11、13、19、20、21 条 → F6.1；第 1、2、14、18 条 → F6.2；
  第 9、12、15、16、17、22 条 → F6.3；第 6 条 → F6.4；第 23 条 → F6.5；第 24 条 → F6.6。
- **范围外调整（D44）**：「mcap / LanceDB 的 reader」不再是范围外：`dev` 上的 PR #155 全量接入 v2，并支持 TOS 上的数据；
  D34 里「新的输入格式代码照搬、默认关闭、预检判为不支持」的做法对 mcap / lance 不再适用，冻结点前移到 `dev@eb637ba40`。
- **F3.2 追加（D46）**：子任务运行期间任务显示「运行中」（显示层，状态机不变）。
- **F3.3 追加（D47）**：人工裁决入口常驻，任务有结果即可进入；仍不跨任务（D32）。
- **F4.1 追加（D48）**：dataverse 的 curation 组件由 curator-v2 替换，部署方式见 D48 与 `deploy/README.md`。
- **F6.6 补充（D48，同日核对集群后）**：旧 v2 的数据盘在 cn-beijing-e，galbot 跑在只有 b / c / d 区的 VCI 上，
  所以不挪盘：galbot 新建 `ebs-essd` 盘，停机把数据拷过去（约 100 MB，核对哈希），v2 跑 VCI；验收③照旧，旧盘留作回滚。
