# Curator v2 前端（W10）

数据质检平台的网页控制台：React 18 + TypeScript（strict）+ Arco Design + React Router 6 + TanStack Query + Vite + ECharts。
构建产物是一堆静态文件，由 Daemon 托管；页面上的所有数据都向 Daemon 要（D17），浏览器只在 `localStorage` 里记几项界面偏好（每页条数、上次用的访问密钥之类）。

设计依据：`docs/design/07-frontend.md`（主规格）、`03-rest-api.md`，契约 `docs/contracts/openapi.yaml`（C4 1.9.0，请求和响应的形状只认它）与 `modules.json`（C1），静态稿在 `mockups/`（只读参考）。

## 目录

| 路径 | 内容 |
|---|---|
| `src/api/` | 由契约生成的类型 `schema.d.ts`（`npm run gen:api`，不要手改）、薄客户端、错误、查询键、SSE |
| `src/pages/` | 页面：概览、数据集、任务列表、新建任务（两屏）、任务详情、质检报告、人工裁决（单个任务的，和跨任务的列表 `adjudication/AdjudicationListPage.tsx`）、系统和资源配置 |
| `src/features/` | 几页共用的块：预检、数据集登记、任务操作、视频签名与多机位同步播放（`media/syncPlayback.ts`）、密钥抽屉 |
| `src/lib/` | 纯函数（深链解析、表单模型、裁决规则、报告取值……），都有单测 |
| `src/locales/zh.ts` | 全部界面文案，一个文件 |
| `src/mocks/` | MSW 模拟：内存里的一套「模拟世界」，每个 C4 接口都有处理器，数字和静态稿一致 |
| `src/test/` | 测试环境：jsdom 补丁、契约校验器、渲染与 Arco 操作的小工具 |
| `scripts/` | 类型生成与校验、`serve-dist.mjs`（按 Daemon 的方式托管构建产物） |

## 本地环境

Node 20.19 及以上或 22.13 及以上（见 `package.json` 的 `engines`）。依赖版本全部钉死，装依赖一律用 `npm ci`：

```bash
cd frontend
npm ci
```

要增减依赖时注意：npm 10.9.2 解析依赖树会崩（`Cannot read properties of null (reading 'edgesOut')`），用 `npx -y npm@11 install <包名>@<版本>` 改 `package-lock.json`；`npm ci` 用 npm 10 没问题。

## 常用命令

| 命令 | 作用 |
|---|---|
| `npm run dev` | 开发服务器 http://localhost:5173/ ，默认接模拟数据 |
| `npm run lint` | ESLint，零警告 |
| `npm run typecheck` | `tsc --noEmit` |
| `npm test` | Vitest 全部单测与页面流程测试（本机约 15 秒） |
| `npm run build` | 生产构建到 `dist/`，不含任何模拟代码 |
| `npm run build:demo` | 带模拟数据的构建（演示用，别部署） |
| `npm run serve:dist` | 按 Daemon 的方式托管 `dist/`：前缀、SPA 回退、可选接口代理 |
| `npm run gen:api` | 从 `../docs/contracts/openapi.yaml` 重新生成 `src/api/schema.d.ts` |
| `npm run check:api` | 生成结果和提交的文件不一致就失败（CI 用） |

## 开发

- **默认用模拟数据**：`npm run dev` 启动后浏览器里由 Service Worker 应答所有接口，SSE 也有模拟。打不开 Service Worker 的环境（一些内嵌浏览器、无痕模式）自动改成在页面里拦截 fetch，此时 SSE 不可用，页面会自己降级成 5 秒轮询 —— 这条路径本身也值得看。模拟世界里的子任务（重试、继续运行、执行裁决、重新导出）会自己跑：排队 3 秒、运行 30 秒，结束后按结果重算任务状态（`src/mocks/subtaskSim.ts`；测试里关着，用 `finishSubtask()` 手动结束）。刷新页面会重置模拟世界，所以跟着步骤走时在页面里点链接，不要直接改地址栏。
- **使用文档**：侧边栏「帮助 · 使用文档」的地址只写在 `src/components/AppLayout.tsx` 的 `HELP_LINKS.docs`，现在留空：点了只提示「使用文档还没配置」；填上地址后在新标签页打开。
- **接真的 Daemon**：`VITE_API_TARGET=http://127.0.0.1:8080 npm run dev`，开发服务器把 `{前缀}/api` 和 `{前缀}/events` 代理过去，模拟数据关闭。
- **挂载前缀**：`CURATOR_BASE=/curation npm run dev`，然后打开 http://localhost:5173/curation/ 。开发服务器在 `index.html` 里做和 Daemon 一样的注入。

## 构建与部署：Daemon 要做的事

`npm run build` 的产物在 `frontend/dist/`：`index.html`、`favicon.svg`、`assets/*`（文件名带内容哈希）。资源路径全部是相对的，同一份产物能挂在任何前缀下。Daemon 托管时要做三件事：

1. **静态文件**：`{base}/` 下按路径返回 `dist/` 里的文件。`assets/*` 可以长缓存（`Cache-Control: public, max-age=31536000, immutable`），`index.html` 用 `no-cache`。
2. **SPA 回退**：`{base}/` 下不是文件、也不在 `{base}/api/`、`{base}/events/` 下的 GET 请求，一律返回 `index.html`（刷新 `/curation/tasks/<id>/report` 不能 404）。`assets/` 下找不到的文件返回 404，不要回退。
3. **注入前缀**：把 `index.html` 里的标记 `<!-- curator:base -->` 替换成

   ```html
   <base href="/curation/"><script>window.__CURATOR_BASE__ = "/curation";</script>
   ```

   部署在根路径时是 `<base href="/"><script>window.__CURATOR_BASE__ = "";</script>`。前缀只能含 URL 路径字符（`[A-Za-z0-9._~-/]`），写进 JS 时用 JSON 编码。`<base>` 必须是静态注入的：浏览器的预加载扫描器只认 HTML 里现成的 `<base>`，否则深链页面会先去错误的路径预取 `./assets/*`。没注入时页面会从地址推断前缀、自己补一个 `<base>`，但那只是兜底。

   可选：在同一处再注入站点开关 `window.__CURATOR_FEATURES__ = { local_input: true, home_region: "cn-beijing" }`（本地输入源是否开放、站点所在地域；C4 没有站点配置接口，见下文「契约缺口」）。

路由器的 basename、接口前缀 `{base}/api/v1`、SSE 地址 `{base}/events/tasks/{id}` 都从 `window.__CURATOR_BASE__` 拼出来，代码里没有写死的路径（ESLint 规则拦着 `/api/`、`/events/` 字面量）。

照着 Daemon 的做法在本机看一遍构建产物：

```bash
npm run build:demo                                   # 带模拟数据
npm run serve:dist -- --base /curation --port 4173   # http://localhost:4173/curation/
# 生产构建 + 真 Daemon：
npm run build && npm run serve:dist -- --base /curation --api http://127.0.0.1:8080
```

## 类型、契约与测试

- `src/api/schema.d.ts` 由 openapi-typescript 从 C4 生成（外部 `$ref` 到 `cli/*.schema.json` 都能解析），客户端 `src/api/client.ts` 在它上面用 openapi-fetch，路径、参数、请求体全有类型；非 2xx 统一变成 `ApiError`（`code` 给程序，`message` 是给人看的中文）。每个写请求都带 `Content-Type: application/json`，没有请求体也带（C4 1.2 起的要求）。改了 C4 要跑 `npm run gen:api`。
- 模拟数据不会和契约走样：`src/mocks/contract.test.ts` 把每个样例和每个接口的模拟响应都拿 C4 校验一遍，页面测试里 UI 发出的每个请求体也会被校验（违反契约的测试直接失败）。C4 1.1.0 有三处 `allOf` 组合了封闭 schema，没有实例能通过；1.2.0 修好了 `Task.vlm`，1.3.0 修好了另外两处，测试按契约版本判断还要不要打补丁。
- `CURATOR_CONTRACTS=<目录> npm test` 用另一份契约跑全部测试（目录结构同 `docs/contracts/`），用来提前试新的契约修订。
- 页面测试用 Vitest + Testing Library + MSW，覆盖：客户端与错误、深链解析（v1 的全部用例）、param_schema 生成的表单、指纹变化对话框、SSE 断开后降级轮询、所有必填项的红 * 与校验、每一页的主要流程、裁决三条纪律、视频地址失效自动重签、多机位同步播放（控制器用假的媒体元素测：不自动播放、缓冲齐了才起播、缺数据全停、跟随暂停 / 拖动、漂移对齐）、报告各模块视图没有 JSON 与逐条清单、Episode 明细的搜索 / 筛选 / 上下条、EEF 模块的裁决卡片与 Episode 明细一块（模拟世界的任务不勾 EEF，这两处由测试注入记录：`src/mocks/eef.ts`）。

## 手动验证（模拟数据）

`npm run dev`，打开 http://localhost:5173/ 。模拟世界讲的是静态稿里那套故事：主角是任务「droid 前 50 条质检」。侧边栏依次是概览、质检（默认展开，下面是「质检任务」「人工裁决」）、数据集、系统和资源配置，下面是「帮助 · 使用文档」（点了提示「使用文档还没配置」）；每页面包屑的「数据质检平台」都能点回概览。任务的裁决页高亮「人工裁决」，报告页高亮「质检任务」。

1. **概览**：标题下没有说明文字；只有并排、等高的两张卡片，没有「待处理」和「数据集」。Token 柱状图的纵轴是 `0 / 5M / 10M / 15M` 这样的 K / M 单位，只有三四个刻度。左边「运行情况」：运行中 1、排队中 1、已暂停 2，点数字进筛好的任务列表；下面是「umi_640 全量质检」和它的进度条。右边卡片的标题是时间范围下拉：默认「近 7 天」，柱状图每天一根（`MM-DD`）；换成「近 1 月」是 30 根；「近 3 月」按周，13 根，标签是周一的日期加「周」；「近 1 年」按月，12 根（`2026-09`），图上方的小标题跟着变成「每周 / 每月的 Token 消耗」，完成的任务、episode 数、通过率、Token 一起变。刷新页面，停在上次选的范围。两张卡片始终一样高（高度由右边那张定）；正在跑的任务多到左边放不下时，列表出现「1 / N」的简洁分页，每页条数按卡片高度算。模拟世界里只有一条在跑，看不到分页，这一条由 `OverviewPage.test.tsx` 覆盖；接真的 Daemon、把 `CURATOR_MAX_RUNNING_TASKS` 调大并同时跑几个任务时可以看到。
2. **数据集**：页头只有标题，没有说明文字。5 个已登记的数据集，droid-200 显示「有变化，待重新预检」。每行的操作是「可视化」「新建任务」和红色的「删除」：「可视化」在新标签页打开同域 ReRun，地址是 `<挂载前缀的上一级>/?url=<编码后的 tos://…/数据集名/?region=地域>`（droid_100 是 `/?url=tos%3A%2F%2Fpai-kit-datasets%2Flerobot%2Fdroid_100%2F%3Fregion%3Dcn-beijing`；开发服务器挂在根路径，打开的正是本控制台的深链，会跳到新建页 —— 根路径部署就是这样，`CURATOR_BASE=/curation npm run dev` 时指向 `/`）；HuggingFace 缓存桶的 libero_10 不带地域。进 droid-200 详情点「重新检查」→ 弹出指纹变化对话框（新增 12 个文件）→「重新预检」后变成「一致」。「添加数据集」：必填项有红 *，填 `tos://pai-kit-datasets/lerobot/brand_new` 和访问密钥后自动预检，保存时按钮保持「保存」只转圈，保存后进详情；同一地址再添加会打开已有的那条。删除 umi_640_notask 会被拒（有未结束的任务在用）。详情页有预检结果、模块可用性、指纹变化记录、跑过的任务，页头有「新建质检任务」（带着数据集进新建页）和「可视化」。模块可用性里「EEF–视频一致性」这样长的模块名不折行；要模型的模块（任务成败判定、技能画像）写「可用」，不再是「还没选 VLM 后端」：详情页看的是 VLM 后端，有一个验证通过的就算可用；在「系统和资源配置」里把后端都弄成验证失败（或删掉）后再看，写「需要补充 · 没有可用的 VLM 后端…」。
3. **任务列表**：页码分页、状态中文名（`completed_with_errors` 显示「错误」）、系统暂停的图标与悬停提示、模块列只放摘要（悬停看全部）、按状态 / 模块 / 名称筛选。行内操作按状态给：运行中的能暂停，系统暂停的不能手动恢复，未结束的不能删除；点「启动」（如 libero-10 抽检）状态立刻变「排队中」，不用等下一次轮询，其余操作（暂停、恢复、停止、继续运行、重试、执行裁决、重新导出）同理。每列都有固定宽度：窗口窄了整表横向滚动、操作列钉在右边，不会因为某一行多一个标签就重排；数据集列表的「最近一次质检」把状态放在任务名下一行。「进度 / 结果」列里未结束的任务是两行：当前档（如「VLM 档」，「终判 + 报告」合并叫「报告生成」、「导出 + 交付核验」合并叫「交付」）带进度条和 410 / 631，下一行是总进度 41%，不写剩余时间。有结果的任务「更多」里都有「人工裁决」，有待裁条目的写「人工裁决（10）」（so101 夜间批次没有待裁，也有入口）。「droid 前 50 条质检」点「更多 → 重试（2 条）」：这一行马上变「运行中」，进度列写「子任务运行中」、下面留着原来的结果，页面每 5 秒刷新；约半分钟后重试结束，这一行变回「已完成」（通过 43 · 拒绝 7）。「删除」的确认框有一个默认不勾的「同时清理交付产物」：勾上后显示 `tos://pai-kit-deliveries/droid-50/20260920-130514/`（下面没有别的说明文字），确定时先清理再删除；没写过交付产物的任务（如 libero-10 抽检）没有这个勾选框。
4. **新建任务**：第一屏填 `tos://pai-kit-datasets/lerobot/droid-200`，选访问密钥，停顿后自动预检，模块卡片显示可用 / 需补充 / 不支持（带原因）。访问密钥下拉里验证通过的排前面，partner-upload 写「（未验证）」，old-ci 写「（验证失败）」且不能选。交付目录的「地域」「访问密钥」下拉第一项都固定写「（同数据集）」，后面是去掉数据集那一项的其余选项（数据集选了 readonly-tos 时，交付的访问密钥是「（同数据集）」、prod-tos、partner-upload（未验证）、old-ci（验证失败））。交付目录访问密钥选 prod-tos，交付目录填 `tos://pai-kit-scratch/x`，失焦后做写探针：写得进但探针对象删不掉，给橙色警告；交付目录改成 `tos://pai-kit-nosuch/out` 报红（存储桶不存在）。整个表单和顶上的步骤条用满内容区宽度，底栏不写「开始前会检查：…」，点「保存为待启动」「创建并开始」时只有按钮转圈，不写「正在创建任务…」。预检结果里没命中语义档案时不写那一行，也没有「未通过预检的模块会在「质检范围」里置灰…」；模型配置下没有解释「模型默认」的小字。「高级设置」展开后：模型调用重试次数上限、超时对冲、CPU 并发上限、VLM 并行度上限四项一行（超时对冲是「开启 / 关闭」下拉框，下面没有说明），各类调用超时一行，两个开关一行、开关紧挨着文字。模型配置默认选中默认模型（模拟世界里是 ark-prod 的 doubao-seed-2-0-pro-260215，下拉里带「（默认）」），可以改；没有默认模型时和以前一样要自己选。第二屏按 param_schema 生成模块参数，运动学极限要选机器人型号或跳过。「EEF–视频一致性」卡片不标橙、不提示要上传的文件，任何预设与「全选可用」都不勾它（要上传文件的模块只能手动勾）；手动勾上后第二屏的模块名是小标题（没有「可选设置」这一层），有 trajectory.json 与「夹爪参考」两个文件项（`format: upload`）：不选文件就保存会提示「请填写trajectory.json」，选一个含 `"truth"` 键的文件会显示定位到样本的错误，选合格文件后显示文件名、sha256 前 12 位与摘要，提交的 `modules[]` 里是 `upload:` 句柄。它先 CPU 比对、再请模型复核（D49），勾上后要选 VLM 模型；「夹爪参考」是一个下拉加一个按钮：默认「观测种子」，下面的说明随选项变；换成「夹爪外观模板」会丢掉已传的种子，同一个按钮传 `gripper-template/1.0` 文件，通过后写「N 个模板条目 · 相机 …」，提交的参数里只有选中的那一种；两种都没给时报「请上传夹爪参考（观测种子或夹爪外观模板，二选一）」。上传时按钮先写「上传中…」，文件发完、服务端校验时写「校验中…」（本地模拟服务几乎瞬间返回，两段一闪而过，逻辑见 `src/api/uploads.test.ts`），这期间「保存为待启动」「创建并开始」不能点；夹爪参考的上传结果在整行下面，和下拉框左对齐；两个文件同时上传也不会互相冲掉；勾它不会连带勾别的模块，只勾「技能画像」也不会勾上「精确去重」。「创建并开始」：droid-200 开始前指纹不一致，弹框确认后重新预检。文案：质检范围的预设写「快速质检」，「高级设置」旁没有说明，Episode 选择第一项是「全部」；第二屏不再列「无需额外设置：…」，只勾时间戳检查时第二屏写「开启的模块都不需要额外设置，可以直接创建。」；「创建并开始」做开始前检查时按钮转圈，旁边不写「正在做开始前检查…」。深链：`/?dataset=tos://pai-kit-datasets/lerobot/droid_100&region=cn-beijing` 会跳到新建页并预填，每个参数都有常驻提示。
5. **任务详情**（从列表点「droid 前 50 条质检」）：页头依次是「查看报告」（蓝色）、「人工裁决（10）」（有待裁条目，也是蓝色）和「更多」，报告概览卡片上不再有「查看详细报告」；报告概览六个数；「分档进度」和「Token 消耗」并排等高（窗口窄于 1100 px 时上下排），分档进度里「报告生成」「交付」各是一根合并的条，不写预计时间；Token 卡片不显示金额，「明细」默认展开，可在按模块 / 按子任务之间切换，末行加粗「合计」（886 · 2.01M · 67.5K · 42.9K · 931.8K），「重试花掉的也算在合计里…」在折叠里；模块表（任务成败判定「2 条待补跑」可展开看是哪几条）；卡片标题旁没有灰色说明；执行时间线横向排成一行、打开时停在最新一端，节点上有标题、时间、「查看 r0001 报告」「当前版本」，说明文字悬停看全文（「so101 夜间批次」的执行裁决那次标着「改标重判：首轮完整流程」）；「更多信息」里的配置和执行计划（执行计划仍按原始档名：终判）：任务配置的属性名和值之间有明显间隔；执行计划全是中文——上限来源写「自动规划」，闸门写「打分 64 · 逐机位复核 64 · 取证仲裁 32 …」「请求合并：不合并」，最后一行是「计划估算：VLM 请求约 780 次，耗时约 15 分 00 秒。粗估：假设选中的条目都通过硬门…」（planner 写的英文说明在页面上翻成中文）；「日志」页签按档筛选（原始档名）、往上翻加载更早的。在这里点「更多 → 重试（2 条）」或「导出」：状态标签变「运行中 · 重试 #2」（或「运行中 · 重新导出 #1」），分档进度换成这个子任务的档并标明「子任务 · 重试 #2」，结束后回到重算后的状态，时间线多出「子任务 · 重试 #2」「重试 #2 结束」和新的结果版本。「so101 夜间批次」没有待裁条目：页头「人工裁决」是普通按钮，点进去待裁决页签只写「没有符合筛选条件的条目」，不再提示去被拒复议（自己点「被拒复议」页签）。运行中的任务（如「umi_640 全量质检」）的分档进度里，数值档 / 帧档 / VLM 档是三张并排的小卡片（在飞、排队、已完成、单条耗时、最近一批），宽窗口下一行放下、数字不折行，下面是三层的时间重叠图；「Episode 流水线」每 3 秒刷新，刷新时表格不动，只在卡片右上角（「N / M 条已完成漏斗验证」右边）转一个小图标，页面不会上下跳；页头显示实时通道状态：SSE 连着时实时更新，断开后自动改成每 5 秒轮询；它没有结果，页头没有「人工裁决」。
6. **质检报告**（从任务详情进入，或直接打开 `/tasks/task_01HXR2D8/report`）：页签依次是「报告」「Episode 明细」「性能剖析」。
   页头没有「小节和本次开启的 N 个模块一一对应」，右上角常驻「人工裁决（10）」（主色，历史版本置灰；打开「so101 夜间批次」的报告是没有数字的「人工裁决」）。
   各卡片右上角没有说明文字（质检总览、数据包完整性、本次质检范围、性能剖析的两张卡片只留按钮）。
   **报告**页签：总览「输入 50 = 判废 7 + 交付 41 + 待补跑 2」、耗时「16 分 58 秒 + 7 分 00 秒 / 主流程 + 重试 #1」（一列放不下，这张卡占两列，不截断）、待补跑一栏列出 ep 7、ep 31（点了进 Episode 明细）、判废原因分布；
   数据包完整性是一行三组的中文「名称 值」（格式、Episode、相机、帧率、机器人型号、语义档案、任务标注、源文件清单、结构校验、预检提示），没有英文键名和 JSON；
   本次质检范围（含未运行的运动学极限）每行一句说明，右上角「全部折叠 / 全部展开」。七个模块小节（顺序同 report.json）每节一行关键数字加一到三张图：
   时间戳的不合格原因与时长分布、运动质量的子项均分（不适用的执行器饱和写明原因）、视觉质量的逐相机分布、同步的判定分布与逐相机典型滞后（绿带是容差）加全库概览表、
   任务成败的判定结论 / 弃权原因 / 各层条数、去重的重复组大小、技能画像的技能分布与子技能分布；**没有任何 episode 清单、明细表或 JSON**。
   每节标题栏最右边「收起」，刷新后仍折叠（按浏览器记住）；从本次质检范围点一个折叠着的模块名，会先展开再跳过去。
   任务成败判定与技能画像右上角「去裁决」，任务成败判定和精确去重另有「可复议（N）」。
   **Episode 明细**页签（`#episodes`，`?ep=N` 选中那一条，地址可分享）：搜索框输入 `12`、`ep12` 或 `ep 12` 都能下拉出 ep 12（带「通过」标签，待裁的还有「待裁」）；
   筛选「全部 / 通过 / 拒绝 / 待补跑 / 待裁决」带条数，「上一条 / 下一条」在筛选后的顺序里走（选「拒绝」后从 ep 12 点下一条到 ep 18）。
   ep 29：判决概要（通过、待裁、两条待人工确认各带「去裁决」、任务文本与来源）、三路视频、证据帧，然后每个模块一块：
   任务成败判定的判定链「打分层失败候选 → 逐机位复核一致判未完成 → 判废护栏拦下 → 取证仲裁未触发 → 转人工」；ep 18 只有时间戳、运动质量两块有读数，
   其余写「未进入这一档」；ep 44 的精确去重写「与 ep 43 字节级完全重复」，点 ep 43 跳过去；ep 12 的同步是「已标注异常（不判废）」，逐相机诊断，
   下面是同步曲线（每路画面运动与机械臂运动，右边互相关曲线、绿带与读数点）；ep 0 写明默认只为值得留意的条目保存曲线。
   **同时播放**：进页面和切换 episode 都不播放、不签名；点「同时播放」后三路一起签名，按钮变「停止同步」，旁边「缓冲中…」直到三路都缓冲好才一起开始
   （模拟地址放不出来，会一直停在「缓冲中…」，自动重签两次后该路显示失败，其余照常）；任一路卡住全部暂停，暂停或拖动一路其它路跟着。
   版本下拉切到 r0001 看历史版本：黄色横幅、技能画像小节失败并给出错误、按钮都不可用。打开「so101 夜间批次」的报告：总览下面写「另有 3 条缺源文件，未参与质检」，
   没有重试入口；完整性一块的预检提示是中文，下面列出 ep 212、587、901 各缺什么文件；这份报告有运动学极限小节（按越限类型、按关节两张图）。
   「性能剖析」页签（地址加 `#perf`）切全部 / 仅主流程 / 子任务；图叫「各阶段耗时对比」，延迟表的列写「耗时」（不再用「墙钟」）。
7. **人工裁决**（报告里点「去裁决」或详情里的待裁决徽标）：一条 episode 一张卡片，卡片写着来源模块；每点一下就保存，顶部计数跟着变。ep 29 点「采纳新标注」后成败问题改成按新标注提问；只有标注分歧一问的 ep 36，点「采纳新标注」或「自行改写标注」后多出「顺手给成败结论（选填）」（来自注册表的 `follow_ups`）：判了就以人的结论为准、不再重判，留空或拿不准就按新标注重判；它不算待裁、不影响「已裁」；刷新后仍是这一块、带着之前的回答（服务端用 `follow_up_of` 标明它由标注的回答带出）；标注的回答一改（改写成另一句也算），之前的成败结论就作废，改回「维持原标注」或整条弃用则整块收起；从改写框点回「采纳新标注」、原样再存一次改写都不会提交（服务端把重复的一笔也当新回答，成败结论会作废）；点「其它原因，整条弃用」后成败按钮置灰并说明原因，再点一次撤销；「拿不准」卡片标橙、仍算待裁。「来源模块」筛选在两个页签各自的筛选行里：待裁决页签和「问题类型」「状态」放在一行，被拒复议页签只列可复议的模块（EEF–视频一致性、任务成败判定、精确去重），切换页签时清掉。卡片每页 10 张（多于 10 张时底部出现分页器，可切 10 / 20 / 50；模拟世界的待裁决只有 7 张，看不到分页器，由 `AdjudicationPage.test.tsx` 覆盖），换筛选回到第 1 页。「被拒复议」页签顶部不再有「这里列出可复议模块拒掉的条目……」的说明，列出可复议模块拒掉的条目（D42）：任务成败判定的 5 条和精确去重的 ep 44；卡片标「可复议」，不算待裁，按钮是「恢复为可用 / 维持拒绝 / 拿不准」；ep 44 的卡片写明「与 episode 43 字节级完全重复」，两条的视频并排放；折叠说明里写着时间戳检查拒掉的那条为什么不在这里。复核种类、标题和按钮取自注册表的 `review_lines`（D43），没有专用视图的种类按目录通用渲染。「执行裁决」的确认框写明要应用几条裁决、其中几条改了标要重判任务成败、人已判了成功或失败的改标不重判；有改标重判时给出口径：默认与旧版一致（只跑多视角打分和逐机位复核两层），可选「按首轮的完整流程重判」，选择随请求体 `relabel_rerun` 提交（D39）。确认后「尚未应用」归零。
8. **人工裁决列表**（侧栏「质检 → 人工裁决」）：默认只列有待裁条目的任务（droid 前 50 条质检、droid-200 抽检），操作列「查看报告」在前，「人工裁决（N）」在后、蓝色；点任务名进它的裁决页，侧栏仍高亮「人工裁决」。切到「全部有结果的」列出所有有结果的任务（可去复议被拒的条目），地址带 `?show=all`。能按名称搜索、页码分页。请求是 `GET /tasks?has_result=true[&pending_adjudication=true]`（C4 1.15）。第三轮加过的「质检报告」列表第四轮去掉了（和任务列表重复）。
9. **系统和资源配置**：页头只有标题；访问密钥页签和新建访问密钥的抽屉里都没有「保存时只验身份…」的说明。两个页签（`#vlm` 直达 VLM 后端），列表只有非敏感信息和验证状态（失败带原因），从不回显密钥。新建访问密钥必填项有红 *，Access Key ID 以 BAD 开头会「保存但验证失败」。删除 prod-tos 会说明它正被未结束的任务使用；删除 ark-ep 先确认一次，服务端答复「被已结束的任务引用」后再确认一次才删。删掉 readonly-tos（只有已结束的任务在用）之后回到「droid 前 50 条质检」详情，会看到「访问密钥已删除」的横幅，点「重新绑定访问密钥」换一个。编辑 VLM 后端可改模型的思考强度和并行度。模型列表每页 10 个，表格上方一个框：拉到了模型列表的后端（ark-prod）是只做查询的筛选框，输入 `d20lite` 只剩 `doubao-seed-2-0-lite-260215`（字母按顺序出现即可），没有添加按钮；拉不到列表的后端（ark-ep）是「输入模型 ID」+「添加」，已经在列表里的名字不能再添加，手填 `bad-model` 会报「最小请求没调通」且不保存。操作列里「设为默认」和「移除」挨在一起，默认的那个是「默认」标签 +「取消默认」+「移除」；设了新的，之前的（ark-prod 的 doubao-seed-2-0-pro-260215）就不再是默认，全平台同时只有一个；「取消默认」之后新建任务又要自己选。
10. **mcap 与 Lance（D44）**：数据集列表里 warehouse_mcap 的格式是「mcap」，格式筛选多了 mcap 与 Lance 两项。新建任务第一屏填 `tos://pai-kit-datasets/raw/warehouse_mcap`：预检写「mcap · 12 条 episode · 2 路相机（front / wrist）」（没有帧率）和「只读每个 mcap 文件的摘要（通道、消息数与元数据记录），未读取消息」，「EEF–视频一致性」在不支持区，原因「该模块只能读 LeRobot 数据集，不支持 mcap」；Episode 选「自选」后预览格子没有首帧，任务一行写「（任务文本在 mcap 文件里，质检时读取）」。填 `tos://pai-kit-datasets/raw/pusht_lance`：预检写「Lance（LeRobot v3 元数据） · 206 条 episode · 1 路相机（image） · 10 fps」和「读取 meta/ 下的 LeRobot v3 元数据，未读取 frames / videos 表」。填 `tos://pai-kit-datasets/raw/warehouse_rrd`：全部模块不支持，原因「当前支持 LeRobot v2/v3、mcap 与 Lance（lerobot-lance-convert 0.3.0 起），检测到 rrd」。报告页的数据包完整性对 mcap / Lance 任务多一行「数据包（mcap / Lance）」：交付形态和 v1 的体检（模拟世界里没有这种任务，逻辑见 `src/lib/containerReport.test.ts`）。

## 契约缺口（C4 1.9.0）与前端的做法

W10 报告的缺口大多已排进契约的 1.5 待修订（`docs/contracts/SUMMARY.md` §九）。在那之前前端这样做：

| 缺口 | 前端现在怎么做 |
|---|---|
| 任务列表不能按「有待裁决」「交付过期」筛选 | 概览已不列这两类（F6.1 起只有「运行情况」和时间段两张卡片），暂时没有页面需要 |
| 任务上没记录用的是哪个预设（完整 / 快速 / 自选） | 按注册表的 `needs` 反推，只读 `needs`、不猜模块 id |
| 没有站点配置接口（HuggingFace 缓存桶是否存在、站点地域、本地输入源） | 能浏览公共数据集就当缓存桶存在；其余读可选的 `window.__CURATOR_FEATURES__` |
| `SignedUrl` 带 `from_ts` / `to_ts`，签名时并不返回；`expires_at` 的单位没写 | 片段时间一律取逐条接口 `EpisodeView.videos[].from_ts/to_ts`；`expires_at` 按毫秒处理 |
| 报告里模块的 `summary` 是自由对象（C2） | 06 §6.2 定了各模块的汇总键（1.9.0 起 CLI 写入），八个模块和 EEF 模块各有专用视图；没有专用视图的模块用默认视图（判决分布、标量、计数字典和 `{name, count}` 数组成图，其余可读地列出）；老报告缺新键时显示已有内容并注明 |
| `report.overview` 没有「平均质量分」（06 §6.2 提到），复议没有「关键读数」；`overview.duration_s` CLI 写不出 | 平均质量分不显示；耗时按执行时间线算这一版背后的主流程与子任务 |
| 某模块出错的是哪几条 episode 没有接口 | 任务详情从该档的 error 日志里找 `episode_index` |
| 裁决卡片不带视频引用 | 每张卡片滚进视口时再调一次逐条接口取视频和证据帧 |
| `listAdjudication` 的摘要写「按来源模块分组」，07 §6 定的是一条 episode 一张卡片、不分组 | 按 07 §6 |
| 裁决的 `source` 只能传一个、`status` 没有「拿不准」 | 多选来源、「拿不准」在前端对已加载的卡片再筛 |
| `DecisionInput` 没有「撤回」 | 撤销「整条弃用」记一笔「拿不准」 |
| 注册表没声明的复核种类（服务端不该发，但前端收到了） | 照样列出这一问和理由，不给按钮，写明「注册表里没有这种复核」 |
| `Subtask.progress` 是自由对象 | 按 `{stages: StageProgress[]}` 读（Daemon 就这样写），读不出就当子任务还没开始 |
| 子任务运行期间的 SSE `progress` 不带 `subtask_id` | 有子任务在跑时，`progress` 记到这个子任务名下（Daemon 此时只跑它），不动主流程的分档进度 |
| 列表行的 `active_subtask` 只有 id | 状态列只写「运行中」，不写子任务名；详情页有完整的子任务，写「运行中 · 重试 #2」 |
| 没有站点配置说明 ReRun viewer 在哪 | 「可视化」按挂载前缀的上一级拼（`/curation` → `/`），见 `src/lib/rerun.ts` |
