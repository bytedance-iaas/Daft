# Curator v2 设计文档评审记录（2026-09-20）

> 评审对象：`docs/design/00`–`11` 共 12 篇（飞书副本 rev 13 与仓库内容一致）。
> 对照物：①需求文档《Physical AI Kit - Curator 设计文档Prompt》；②v1 代码（本分支 `robot-curation/curation/`）；
> ③v1 使用文档《DataVerse 案例实践》「质检平台（Curation）」章节；④火山方舟官方 API 文档（2026-09-20 查阅）。
> 用法：每项有编号，澄清后按编号逐项修订；「状态」列只取 `待澄清` / `待修订` / `已修订` / `不改`。

## 0. 结论先行

设计文档的分层、契约冻结顺序、对账思路是对的，可以继续用。问题集中在三类：

1. **对 v1 的几处关键描述与代码不符**（A 类，19 项）。其中 VLM 调用图、解码方式、数据集级模块的执行位置
   这三处是地基，建在上面的「请求合并」「decode stage + 帧缓存」「stage 计划」都要跟着改。
2. **文档内部不自洽或契约没写到可编码**（B 类，21 项）。
3. **需求里点了名、文档没落地的**（C 类，12 项），例如方舟参数核对、火山术语、任务的「改」。

需要需求方拍板的 8 个问题（§1）已全部有结论，记为 D16–D23；其余按默认处理意见修订，默认项汇总在 00 篇 §7.1，需求方可以否掉。

**状态（2026-09-20）**：A / B / C 各项均已落到 12 篇 Markdown 里。

---

## 1. 需要拍板的问题

### Q1 VLM 请求合并的一期范围 · 结论：A（记为 D23）

**事实**（v1 代码）：04 篇 §4.1 的前提是「同一条 episode 的同一批抽帧，本来要被 task_success、skill_profile 各问一遍」。
代码里不是这样：

| 调用 | 次数/条 | 图片 | 帧怎么来 | 条件 |
|-|-|-|-|-|
| task_success · probe | 8 次（每个采样时刻 1 次，顺序打乱） | 每次 2×机位数 张（参考帧 + 该时刻） | 0.5s 间隔解码，≤4 机位（`funnel.py:782-790`，`task_success.py:74-77`） | 每条必跑 |
| task_success · endstate | 每机位 2 次（完成?/失败?） | 每次 8 张，单机位 | 复用 probe 已解码的帧（`task_success.py:333-338`） | 每条必跑，不看 probe 结果 |
| task_success · 任务类型 / 判废护栏 / 取证仲裁 | 若干 | 仲裁用裁剪放大图 | 同上 | 分别在关键词未命中 / 判废且为原始标注 / 弃权时触发 |
| skill_profile · caption | 1 次 | ≤4 机位 × 8 帧 | **另一次全帧率解码**后 linspace 取 8 帧（`caption.py:95-106`） | 只对判决 = keep 且去重后的条目跑（`run.py:823-883`） |
| skill_profile · 归纳/审计 | 数据集级，纯文本 | 无 | — | — |
| 漏斗前 · 无标注补 caption | 1 次 | 同 caption | 同 caption | 只对没有任务标注的条目，漏斗前全量跑（`run.py:700-724`），结果被 skill_profile 复用 |

两个模块的帧集不同、执行时机不同、调用结构不同（一个是十几次请求的证据链，一个是单次请求），
不存在「同一批帧被问两遍」。现有两个模块之间几乎没有天然的合并对象。

**选项**：
- **A（推荐）**：一期交付合并**框架**（MergeUnit / MergeStrategy / MergeLimits / 按 key 拆回 / 单项降级单发 / usage 摊派），
  用契约测试加一个示例 VLM 模块验证；现有两个模块保持 v1 调用图不动，对账零风险。
  新 VLM 模块按「单次请求、声明帧策略」的规范接入，帧策略相同的自动合并 —— 这正是需求里「5 个模块合并」的场景。
- B：在 A 之上，把 caption 并进 task_success 的某次请求。需要统一抽帧，caption 的输入随之变化，
  技能画像与 v1 不再可比，要接受对账口径放宽；而每条只省 1 次请求（约 1/15）。
- C：连模块内部的多次调用也合并（probe 8→1 等）。这是改算法，违反「实现思路不能改」，不推荐。

**影响**：04 篇 §4、§3 计划示例、05 篇 §1、10 篇 §3.4、W6 范围。F2.4 验收①在 A 方案下由示例模块承担。

### Q2 解码与分档：去掉 decode stage 和帧缓存？ · 结论：A（记为 D18）

**事实**：v1 没有任何帧缓存，帧只在内存里，用完即释放（`funnel.py:466-468, 557`）。
- 视频-动作同步必须**全帧率**解码（lag 分辨率 = 帧间隔，`funnel.py:512-528` 注释），视觉质量从同一批帧里按 0.5s 抽稀，
  两者在同一个 UDF 里共享一次解码、逐机位串行。
- task_success 自己再解码一次（0.5s 间隔，448px，≤4 机位）；caption 第三次（全帧率）；证据帧导出第四次（首个机位）。

设计里的 `curation decode`（00 §4 ③、02 §3.3、04 §3 的 decode stage、09 的 200Gi 帧缓存）要把「三方复用的帧」落盘：
全帧率帧单条 episode 就是 GB 级（60s × 30fps × 3 机位，448px 原始约 1.8GB），640 条约 1TB；
用 JPEG 压缩又会让视觉质量、同步的读数偏离 v1，「逐位一致」直接不成立。

**选项**：
- **A（推荐）**：删掉 decode stage 和帧缓存。`check` 允许一次传**同一档内**的多个模块
  （`--modules visual_quality,video_action_sync`），同进程共享一次解码，结果仍按模块分文件落盘；
  单独重试其中一个模块时接受重复解码。档的划分照搬 v1：数值档 → 硬门① → 帧档 → 硬门② → task_success → 判决 → 去重 → 技能画像。
- B：保留 decode stage，但只缓存 VLM 用的抽稀帧（0.5s / 448px，体积可控，且须无损格式）；视觉/同步仍在进程内全帧率解码。
- C：维持原设计。

**影响**：00 §4、02 §3.3–3.4、04 §2–3、09 §2.1（cache 卷）/§5、11 篇 W3 验收。

### Q3 黄金对账数据集 · 结论：A，双基线（记为 D19）

**事实**：`umi_640_notask` 在 v1 代码/README/测试里没有任何出现，只存在于部署环境。按代码推断它跑不到这些路径：
- `kinematic_limits`：`umi` 不在运动学规格库（9 款里没有，`registry/profiles/`），**整项跳过** —— 对账表里这一行是空转；
- 原始标注路径：无标注 → 全部走「自产 caption」兜底，判废护栏（要求 `task_src == "原始标注"`）、标注分歧检出、
  「标注分歧」裁决线都不会触发；
- 取证仲裁的外部机位线：umi 两路都是腕部相机（`dataset_profiles/umi.yaml`）；
- `motion_quality` 的卡死/饱和子项：umi 的 action ≡ 下一帧 state，同源自弃权（`motion_quality.py:266`）。

**选项**：
- **A（推荐）**：增加第二基线 `droid_lerobot` 前 50 条（使用文档同款：franka 在规格库、3 机位含外部机位、
  50 条里 22 条无标注 + 28 条有标注），两个基线都要过。D15 与 F1.1/F4.2 追加一条，不改原有条款。
- B：只用 `umi_640_notask`，在 10 篇里写明覆盖缺口，缺口部分靠单元测试兜底。
- C：换别的数据集（请指定）。

### Q4 需求里的「自定义」提交方式，要不要留一个入口 · 结论：不留（重申 D5）

需求原文：前端「先使用后端的 helper API 拿到并行优化后的参数后，再通过"自定义"的方式提交任务」。
D5 已定为「只有标准模式，helper 在 Daemon 内部必经」。我不重开 D5，但它留下两个实际问题：
①「合并粒度、并发度调优」是需求点名的后续独立任务，调优要反复提交不同计划，没有入口就只能改服务端配置；
②文档说 planner 的决策「可观测、可干预」，但没有任何接口能看到计划。

**选项**：
- **A（推荐）**：UI 和普通调用方仍只有标准模式；加只读的 `GET /api/v1/tasks/{id}/plan`；
  `POST /tasks` 接受可选的 `plan_overrides`（合并策略、各档并发、批大小），标注为调优/调试用，UI 不露出。
- B：严格按 D5，不留入口；调优时改站点配置。
- C：回到需求原文的两步式（`POST /plan` 拿参数 → 带参数提交）。

**需求方答复（2026-09-20）**：「这里我想多了。所有提交都走同一个标准的接口 `run_modules()`。后端的 `run_modules()` 实现里都需要先用 helper 优化一下，把能够合并的 VLM 请求合并一下，然后用最佳的并发方式来跑。」—— 不提供 `plan_overrides`，也不做两步式；生成的计划只读可查（`GET /tasks/{id}/plan`），调优经站点配置。

### Q5 任务的「改」与失败/停止后的续跑 · 结论：A（记为 D20）

需求要求任务「增删改查」，且「错误」任务可重试。文档现状：创建后只能改名；`stopped` / `failed` 是终态，
`POST /retry` 只定义了「重试失败模块」，对任务级 `failed`（没有模块结果）和 `stopped`（跑了一半）没有语义；
`start_now=false` 的任务停在 `queued`，和「排队等 worker」共用一个状态。

**选项**：
- **A（推荐）**：加「待启动」状态（`start_now=false` 进入，可改全部配置，启动后锁定）；
  `stopped` / `failed` 支持「继续运行」= 建 `resume` 子任务，只跑没完成的模块 × episode；另加「复制为新任务」。
- B：不加状态。创建后只能改名；`stopped` / `failed` 只能「复制为新任务」从头跑。
- C：只做「继续运行」和「复制为新任务」，不做待启动状态（`start_now` 参数删掉）。

### Q6 任务列表的分页形态 · 结论：A，页码分页（记为 D21）

需求：「标准的分页逻辑和 Lazy Loading」，同时要求使用习惯对齐火山控制台。文档与静态稿选的是游标分页 +「加载更多」。
火山控制台的表格一律是页码 + 每页条数 + 总数。这会改 REST 契约（C4），要在冻结前定。

**选项**：
- **A（推荐）**：任务列表用页码分页（`page` / `page_size` / `total`，任务量级下 OFFSET 没有性能问题）；
  Lazy Loading 用在重内容上：episode 预览图、报告明细表、视频、日志；裁决队列和日志流保留游标。
- B：维持游标 +「加载更多」。
- C：API 保留游标并返回 `total`，UI 只给「上一页 / 下一页」。

### Q7 Agent 入口：links 由谁产出 · 结论：A（记为 D22）

02 篇 §6 让「任何 CLI 命令的 `--json` 输出」带 `links`，指向 `/tasks/<id>/report`。但原子 CLI 不经过 Daemon，
没有 task 概念，直接跑出来的结果在 Web 上没有对应页面。

**选项**：
- **A（推荐）**：Agent 主入口定为 REST API；CLI 增加一组薄客户端命令（`curation task create|get|wait|retry|adjudication`），
  内部调 Daemon 的 REST，返回带绝对 URL 的 `links`；原子命令（preflight/check/...）不产出 `links`。
- B：原子 CLI 直跑的结果也要能在 Web 打开 —— Daemon 提供「把已有 run 目录导入为任务」的接口。
- C：只有 REST 带 `links`，CLI 不加客户端命令。

### Q8 文档落点 · 结论：A，仓库为源、同步飞书

飞书文档头写明「以仓库 Markdown 为准，本文档为汇总副本」。

**选项**：
- **A（推荐）**：改仓库 12 篇 Markdown 并 commit，再把改动同步到飞书副本的对应章节（保留文档链接和已有评论）。
- B：只改飞书文档。
- C：只改仓库，最后整体重新导出一份新的飞书文档。

---

## 2. A 类：与 v1 代码事实不符

| # | 问题 | 依据 | 建议 | 涉及 | 状态 |
|-|-|-|-|-|-|
| A1 | VLM 调用图被简化成「同批帧问两遍」，合并设计建在错误前提上 | 见 Q1 | 按 Q1 结论重写 04 §4；补一张真实调用图 | 04 §2.2/§3/§4，05 §1 | 已修订 |
| A2 | 「一次解码三方复用」不成立；无帧缓存 | 见 Q2 | 按 Q2 结论改 | 00 §4，02 §3.3，04 §3，09 | 已修订 |
| A3 | `dedup`、`skill_profile` 的执行位置错了：文档把 dedup 放在 cpu-numeric 档跑全量、skill_profile 与 task_success 同档。v1 是判决之后，dedup 只对 keep 集合跑（先动作哈希，碰撞组再算视频内容哈希），skill_profile 对 keep − 重复项跑。且 **dedup 会判拒**（重复项进 reject，理由「拒绝(去重)」），不是 `gate=none` | `run.py:823-883`，`report.py:795-798`，`config.py:12-15, 206` | 注册表增加 `phase: funnel \| post_verdict` 与模块依赖（task_success → 判决 → dedup → skill_profile）；`aggregate` 拆成「漏斗判决」与「终判」两步；子任务/裁决改变 keep 集合后，dedup 与 skill_profile 自动增量同步（复用已有 caption，走 v1 的 `reassign` / `reprofile`，不重新归纳体系）。这也是 `stale` 状态真正的用途 | 00 §4，04 §3，05 §1，06 §3，01 §2.4 | 已修订 |
| A4 | 「三份清单互斥且完备」不对。v1 里弃权条目判决仍是 keep、**计入交付**，`review.json` 是带未决问题的叠加视图（使用文档：交付 43 条，其中 10 条待人工）。按文档「按 passed 导出」会少交付这部分 | `verdict.py:16-33`，`report.py:803-823` | 改为：passed 与 reject 互斥完备；review ⊂ passed；导出含 review 条目，人工判失败/弃用后经重新导出剔除 | 06 §3、§4.1 | 已修订 |
| A5 | 整段缺失「无标注补 caption」（使用文档称「任务打标」）：无任务标注的条目在漏斗前由 VLM 生成描述，作为 task_success 的任务文本，并**写回交付数据集**的 instruction（带来源）。文档却把 `instruction` 缺失判为「模块不可用」—— 按此规则 `umi_640_notask` 的两个 VLM 模块会被标灰 | `run.py:18-29, 700-724, 1412-1427` | 增加 `autolabel` 前置步骤（归属 task_success/skill_profile 的共享依赖）；预检对无标注给提示「将由模型补充任务描述」而不是标灰；D15 的「VLM 三项」应指 打标 / 成败 / 画像 | 04 §2，05 §2–3，00 D15 | 已修订 |
| A6 | 黄金数据集覆盖缺口 | 见 Q3 | 按 Q3 结论 | 10 §3，00 D15 | 已修订 |
| A7 | 对账工具假设 v1 产出 `checks/<module>/results.jsonl`。v1 没有这个文件，也没有 `report.json`（残留会被主动删除）和 `_COMPLETE`（完整性靠 `meta/info.json` → `passed.json` → `latest` 的延后上传顺序）。v1 的明细 CSV 还可能做过取整 | `report.py:800-802`，`tos_store.py:190-226`，`delivery.py:57` | W0 增加 `tools/parity/dump_v1.py`：在 `release_v1` 检出上调用 v1 内部函数，把各检查的原始结果（未经报告格式化）导出成规范化 `results.jsonl`；对账比较「规范化记录」而不是两边的交付文件 | 10 §3.1–3.3，11 W0 | 已修订 |
| A8 | 并发数字过时：「32×8=256 在飞零 429」已被同一文件后续注释推翻。现状是三把独立闸门：probe 64（进程级）、endstate 64（=episode 并发×2）、仲裁 32，实测方舟在飞 117 零 429（2026-09-07）。另有 caption 32、llm 16、audit 16 | `default.yaml:11-18, 108-117, 292-299`，`funnel.py:867` 注释已过时 | 改 04 §1、§2.2 的表，列全八把闸门及默认值 | 04 §1–2 | 已修订 |
| A9 | v1 **不传任何思考参数**（请求体只有 model/temperature/max_tokens/messages），也不解析 `usage`。前者意味着对账跑批必须不传 `reasoning_effort`（走模型默认），否则输入形态就变了 | `vlm_client.py:817-818` | `thinking_effort = null` 定义为「不传该字段」，对账固定用 null；见 C1 | 01 §2.2，04 §5，10 §3 | 已修订 |
| A10 | v1 的重试不是「1/2/4s × 3」一种：图片请求走 `hedged_request`（T 时刻对冲补发一次，5xx/连接错立即串行重发，**429 不重发**，双双超时后再给一次），纯文本 `llm_ask` 才是 1/2/4s 退避最多 4 次。文档只说「沿用 v1 的 hedging」+ 新增 `vlm_retry`，没说两者怎么叠 | `vlm_client.py:267-432, 1176-1224` | 写明：`hedged_request` 作为内层传输保留；`vlm_retry` 是外层逻辑重试（内层放弃后才触发，覆盖超时/连接错/5xx/429，遵守 Retry-After）；CLI 默认 `--retry 0` 且不对冲，Daemon 传 `--retry 3 --hedge`；报告里两层分列 | 04 §6，02 §3.4 | 已修订 |
| A11 | 人工裁决的 v1 语义比文档多四件事：①「执行裁决」会把改标**写回交付数据集**（`task_overrides`）并同步技能画像（只重排被裁决的条目）；②结束前做交付核验（逐文件回读，FSX 可见性延迟 20–60s）；③改标后重跑 task_success，但同一条已有人工成败结论时**不重跑**；④裁决跨批次保留但 `run` **从不自动应用**，新批次里它们显示为「沿用自此前的人工裁决 · 待应用」。另有 `--retry-abstained`：只补判因 VLM 调用/解析失败而弃权的条目 | `rejudge.py:674-766, 1000-1105`，`decisions.py`，`run.py:109-202` | 06 §5 补全；`apply_adjudication` 子任务 = apply → （按需）重跑 task_success → 终判 → 画像同步 → 报告；导出仍按 D9 由用户显式触发，改标随导出写入；加 `curation verify`（交付核验）原子命令；`retry-abstained` 并入「重试」（见 C6） | 06 §5，02 §3.8 | 已修订 |
| A12 | `/media/sign` 限定「路径必须在交付目录前缀内」。v1 的视频来源链是 审片片段 → 交付数据集 → **源数据集**（被拒条目只在源数据集里有画面） | `manifest.py:3569-3588, 2752-2773` | 允许两个前缀：任务的交付目录（输出凭证）与输入数据集（输入凭证）；v3 源用 `#t=from,to` 片段；公网端点签名（pod 内网端点对浏览器是死链） | 03 §7，08 §6 | 已修订 |
| A13 | 深链细节缺失：键还有 `tos_endpoint`、`source=public`；多值是**合并去重 + 逗号拆分**（可一次带多个数据集）；`endpoint` 只用于提示地域冲突、绝不用于读数据；未知桶绝不回落到默认桶；只代填交付**目录**不填交付名；「键出现但值非法」必须有可见提示。部署上 APIG **不剥前缀**，v1 把全部路由注册在 `--root-path`（如 `/curation`）之下，`/healthz` 与 `{root}/healthz` 都豁免鉴权 | `runner.py:873-1001, 1498-1585`，`app.py:3151-3306, 4847-4950` | 07 §2.1 按代码补全规则表；新增「挂载前缀」：`CURATOR_BASE_PATH`，路由/静态资源/SSE/探针全部挂在前缀下，index.html 注入运行时 base，SPA 回退；旧入口 `{base}/?dataset=…` 302 到 `{base}/tasks/new?…`；多数据集见 D-3 | 07 §2.1，03 §1，09 §2 | 已修订 |
| A14 | 「HF 镜像」在 v1 是两个功能：①`public_datasets` —— **匿名直读**公共桶（站点配置 bucket/region/prefix/manifest/label，无需任何凭证，只读，绝不作交付目录），新建页上是「私有 / HuggingFace 缓存桶」单选；②`curation fetch` —— 调外部 `oniond` 把数据集下载到自己的桶，只有 CLI，无 UI | `public_catalog.py`，`tos_store.py:450-478`，`fetch.py:196-201` | 名称统一为「HuggingFace 缓存桶」（需求用语）；来源选它时「访问凭证」非必填；values.yaml 加 `publicDatasets.*`；`GET /datasets?source=public`；`fetch` 保持 CLI-only（终端下线后经 `kubectl exec`），镜像保留 `oniond` | 00 §5，02 §3.9，07 §3，09 §2.1 | 已修订 |
| A15 | v1 **没有** episode 预览图（只有文本框），需求要「在预览图里勾选」—— 这是新能力，文档和静态稿画了网格，但没有任何接口支撑 | `app.py:2116-2117` | 加 `GET /api/v1/datasets/episodes`（游标分页，返回 index/时长/任务文本/各机位视频的预签名 URL 与 from_ts）；前端用 `<video preload="metadata" src="…#t=">` + 视口懒加载出首帧，服务端不解码。编号以整数 index 为准（`3,10-12`），`ep000034` 只是展示形式 | 02 §3.1，03 §2，07 §3 | 已修订 |
| A16 | v1 能力没有逐项交代去向 | `cli.py:180-411, 696-724` | 10 篇加「v1 能力去向表」。我的默认意见：`--lite` → 新建页「快速质检」预设（只勾 6 个非 VLM 模块）；`--max-episodes` → episode 选择增加「前 N 条」；`--report-only` → 任务参数 `export=false`，之后可用「导出」补做；`public` → `datasets list --source public`；`reprofile` → 保留为内部步骤；`ls` → 保留；`review-page` → 静态审片站下线，只保留片段生成；`--batch` 多数据集 → 见 D-3；`rrd` 输入 → 维持 v1 现状（代码保留，默认关闭）；同步证据图模式（flagged/all/none）→ 模块参数 | 10 §2，02 §3.9 | 已修订 |
| A17 | v1 运行期还有三样「预检」文档没提：数据集语义 profile（`ingest/dataset_profiles/*.yaml`，9 个）、动作语义预检（profile 未命中时用样本数据验 5 种假设，起因是 libero 被误判 5/5 判废）、摄入 schema 校验（拒收并给清晰错误） | `semantics_preflight.py`，`validate.py` | 新建页预检保持只读 metadata，但要跑 `validate_info` 并回报命中的 profile；动作语义预检留在运行期，原样搬运，结论进报告「数据包完整性」；05 §3 写清两者分工 | 05 §3，10 §2 | 已修订 |
| A18 | 报告内容只给了 JSON 骨架。v1 报告有：数据包完整性、总览（输入 = 判废 + 交付）、**轨迹页**（逐条判决卡 + 多机位同步播放 + 逐维读数）、技能分布（条形图 + 两级体系表）、同步曲线（逐相机诊断、筛选）、卡顿时间线（三色条、排序）、明细 CSV、性能剖析（后端/容器配额/延迟分位）。「每模块一个默认表格」撑不起这些 | `report.py:246-676`，`manifest.py`，使用文档 §3 | 06 §6 加「模块小节内容表」（8 行：摘要指标 / 图 / 明细列 / 证据）；报告页保留逐条下钻（从任一模块明细点进某条 episode 的全模块视图），不与「模块一一对应」冲突 | 06 §6，07 §5 | 已修订 |
| A19 | 鉴权搬运细节：环境变量名（`CURATION_UI_HTPASSWD_FILE/USER/PASSWORD`）、realm 字符串、与 rerun viewer 的 nginx 共用同一张 htpasswd 表（同域免二次登录）、只配用户名或只配密码视为未启用、apr1 + bcrypt | `ui/auth.py:39-44, 217-257` | 08 §5.1 写明沿用变量名与 realm（可另设 `CURATOR_*` 别名） | 08 §5 | 已修订 |

## 3. B 类：文档内部不自洽 / 契约不可编码

| # | 问题 | 建议 | 涉及 | 状态 |
|-|-|-|-|-|
| B1 | D15 写「CPU 五项 + VLM 三项」，10/11 篇写「CPU 五项 + dedup 逐位一致、VLM 两项」 | 统一为：确定性六项（含 dedup）逐位一致；VLM 三项（打标/成败/画像）判决级比对 | 00 D15，10 §3.2，11 §5 | 已修订 |
| B2 | 示例自相矛盾：预检示例 `robot_type: "umi"` 却给 `needs_input`（应为 `unsupported`）；报告示例里纯 CPU 的 `video_action_sync` 报 "VLM endpoint unreachable"；新建页线框把「精确去重」画成不支持（文档自己说 `raw_bytes` 总是满足） | 改示例 | 02 §3.1，06 §6，07 §3 | 已修订 |
| B3 | 「（D：…）」悬空引用 3 处，决策表里没有对应条目 | 补 D16（浏览器经预签名 URL 直连 TOS）、D17（前端零业务状态），替换引用 | 03 §7，07 §1，08 §6，00 §7 | 已修订 |
| B4 | 00 §4「前一档的 reject 名单作为后一档的 `--episodes` 输入」 | 应为「幸存者名单」 | 00 §4 | 已修订 |
| B5 | 飞书副本把 `/healthz`、`/readyz` 粘成了 `/healthz/readyz`（导出问题，仓库正确） | 根因：飞书会把只隔一个空格的两个相邻行内代码并成一个。源文件改为用顿号分隔，副本已修正 | 03 §2、08 §5.1 及飞书副本 | 已修订 |
| B6 | `check` 契约是「一次一个模块」，plan 里 VLM 档却是多模块 + 合并分组 —— 合并请求只能在同一个 CLI 进程里发出，两者矛盾 | 随 Q1/Q2：`check --modules` 接受同档多个模块，加 `--plan-stage <file>` 承载合并分组 | 02 §3.4，04 §3 | 已修订 |
| B7 | 「档间流水」与批处理式的原子 CLI 没有对上：批粒度、多次调用如何写同一模块的结果、并发写、断点续跑都没定义 | 最终处理与最初建议不同：**取消档间流水**（v1 也没有；最多省约 4% 墙钟，却带来批边界等待和并发写）。一档一个进程，结果逐条追加到 `parts/<part>.jsonl`；`check --resume` 跳过已有结果的 episode；暂停 = SIGTERM 收尾退出，恢复 = 同一命令加 `--resume`；part 只在续跑和重试时才多于一个 | 02 §3.4，04 §2，06 §1 | 已修订 |
| B8 | 状态机缺口：`paused → stopped`、`pausing → stopping` 没有；`queued` 兼作「未启动」；Daemon 崩溃重启后 `running/pausing/stopping` 怎么对账没写；滚动升级造成的暂停要不要自动恢复没写；子任务之间、子任务与父任务状态的约束没写；子任务失败时旧结果的去留没写 | 补迁移；`pause_reason: user \| system`，system 暂停在启动后自动恢复；启动对账：`running/pausing` → `paused(system)`，`stopping` → `stopped`；同一任务的子任务串行；各类子任务允许的父状态列表；子任务产物先写临时目录，成功才替换 | 01 §3 | 已修订 |
| B9 | `task_module.state = stale` 的解释（驱动「需重新导出」提示）与 `task.delivery_stale` 重复 | `stale` 只表示「该模块的输入集合已变，结果待同步」（见 A3）；交付过期只看 `delivery_stale` | 01 §2.4 | 已修订 |
| B10 | 优雅停机：120s 内没退出的子进程被 SIGKILL 后任务置 `failed`。跑了几小时的任务会因为一条 episode 没收尾而失败 | 置 `paused(system)`：结果是逐条追加的，被杀的那条恢复时重跑即可；前提是结果行原子写（整行 + fsync） | 09 §2.3 | 已修订 |
| B11 | Helm：①`job-migrate` hook 与「启动时迁移」重复，且 Job Pod 挂不上 RWO 的 SQLite 卷（pre-install 时 PVC 还不存在）；②单副本 + RWO 卷用默认 RollingUpdate 会卡死或双写，需要 `strategy: Recreate` 或 StatefulSet；③没写 SQLite 卷必须是块存储 —— WAL 依赖共享内存，NAS / FSX 类网络盘会损坏；④主密钥轮换 Job 同样的挂载问题；⑤`limits.memory: 120Gi` 超过 128G 节点的 allocatable | 删 migrate Job；StatefulSet(replicas=1)；部署前检查加「数据卷 = EBS 块存储」；轮换改为 Pod 内管理命令；limits 改为按 allocatable 的比例并在 README 说明 | 09 §2 | 已修订 |
| B12 | run 目录到底在哪没说清：CLI 收 `--run-dir`，01 §5 说产物在 TOS，v1 是本地缓存目录 + 随产随传 + 末尾核验。同步时机、Pod 重启后从哪恢复、几天后的子任务怎么回灌、emptyDir 丢了怎么办都没写 | 定义：工作目录在数据卷（PVC）`runs/<task_id>/`；模块完成即上传该模块产物；恢复与子任务先查本地、缺失则从 TOS 回灌（搬 v1 `mirror_run` / `sync_back` 及其跳过表）；任务终态 N 天后清本地；数据卷容量按此重估 | 00 §4，01 §5，02，09 §2.1 | 已修订 |
| B13 | 凭证删除：文档说「被运行中任务引用 → 409」，但 `task.input_cred_id` 是外键，历史任务的报告读取和媒体签名还要用它 | 被运行中任务引用 → 409；被历史任务引用 → 二次确认后允许，外键置空，报告页提示「凭证已删除」并支持给任务重新绑定凭证 | 01 §2.3，08 §5.3 | 已修订 |
| B14 | `preflight_mismatch` 无从比较：创建任务的请求体里没有预检快照或其标识 | `POST /preflight` 返回 `preflight_id`（服务端缓存 30 分钟），创建任务时带上；过期或数据集已变则要求重检 | 03 §3 | 已修订 |
| B15 | 需求要求主 API 能传「每个模块传入的参数」，请求体的 `modules` 却是 id 数组；`params` 的全集没枚举；「调用方不传并发参数」与 01 §2.3 注释里的「并发上限」、新建页「高级」里的「并发上限」矛盾 | `modules: [{id, params?}]`（兼容纯字符串）；03 篇列出 `params` 全表（键 / 类型 / 默认 / 对应 v1 配置键）；并发类参数只经 Q4 的 `plan_overrides` | 03 §3，01 §2.3，07 §3 | 已修订 |
| B16 | `credential(kind=ark)` 与 `vlm_backend` 是两个实体、两组端点，UI 上却是「加一个方舟 Key」一步 —— 前端要自己编排两次调用，违反 03 §1。另：任务级 `thinking_effort` 与模型级配置谁覆盖谁没写；模型 / 后端 / 任务三层并发上限怎么合成、怎么映射到 v1 的八把闸门没写 | `POST /vlm-backends` 内联接收 api_key，服务端建凭证；`/credentials` 只管 TOS；优先级：任务 > 模型 > 模型默认（null）；给出「一个并行度 N → 各闸门」的推导表，N=64 时等于 v1 默认 | 01 §2.1–2.2，03 §2，04 §2.2，08 | 已修订 |
| B17 | `links`：CLI 无 task 概念（见 Q7）；03 §3 示例是相对路径，02 §6 说绝对 URL | 随 Q7；统一为绝对 URL（含挂载前缀），未配置 `publicBaseUrl` 时返回相对路径并标 `"absolute": false` | 02 §6，03 §3 | 已修订 |
| B18 | 端点缺失：vlm-backends 的改/删、单模型配置（思考强度/并行度）、自定义端点手动加模型；任务日志；episode 列表（A15）；任务计划（Q4）；公共数据集目录（A14）；任务详情的响应 schema | 补进总表；日志：每个 stage 的完整日志落工作目录并上传，`GET /tasks/{id}/logs?stage=&cursor=`，SSE 只是实时通道 | 03 §2 | 已修订 |
| B19 | `token_usage` 每次请求一行会把单写者队列打满；方舟 `usage` 里还有 `completion_tokens_details.reasoning_tokens`、`prompt_tokens_details.cached_tokens`，思考强度是一等配置，应单列 | 按（子任务, 模块, 调用种类, 模型）聚合，每 5s 刷一次；加两列 | 01 §2.6，04 §5 | 已修订 |
| B20 | `adjudication` 表没有唯一约束，也没有历史；`delivery_key` 跨任务累积，但两个任务若输入不同数据集却写同一交付目录，episode 编号会撞 | 追加式写入、读取时后写者胜（与 v1 CSV 语义一致）；同一交付目录绑定首个任务的输入数据集，不一致则拒绝创建 | 01 §2.7，06 §5 | 已修订 |
| B21 | 08 §5.3 引了一句「需求要求任务密钥需要按用户隔离」，原始需求里没有；同时又说 owner 恒为 `default` | 改为：本期 htpasswd 多账号共享全部资源，账号名只用于审计（`decided_by` 等）；owner 过滤的代码路径照写 | 08 §5.3 | 已修订 |

## 4. C 类：需求覆盖缺口

| # | 问题 | 建议 | 涉及 | 状态 |
|-|-|-|-|-|
| C1 | 需求点名「参考 Doubao 的 API 文档，确定参数/模型列表是正确的」，文档没有体现。查证结果：①`reasoning_effort` 是 **7 档**（none/minimal/low/medium/high/xhigh/max），所有支持该字段的模型都接受 7 档，但按模型映射：doubao-seed-2.0/1.8/1.6 系列默认 medium，2.1 系列默认 high，minimal = 关闭思考，none → minimal，xhigh/max → high；GLM、DeepSeek 的映射各不相同；②另有 `thinking.type`（enabled/disabled/auto），由 `extra_body` 传；③官方「获取基础模型列表」（ListFoundationModels）**只支持 AK/SK 鉴权**，API Key 调不了；OpenAI 兼容文档里没有 `/models`；v1 的做法是先试 `GET {endpoint}/models`，不通就退回一次最小 chat 探活；④当前最新为 doubao-seed-2-1-pro-260915 | 08 篇加「方舟参数映射」小节：DB 存 API 原生 7 档 + null；UI 按模型族只展示有效档位（能力表随版本维护，ConfigMap 可覆盖）；自定义端点不保证支持该字段，默认不传。模型列表：先试 `/models`，失败回落到内置视觉模型清单 + 手填 Model ID / Endpoint ID（`ep-…`），并用一次最小 chat 请求校验所选模型已开通 | 01 §2.2，07 §7，08 §4 | 已修订 |
| C2 | 需求要求术语对齐火山，文档通篇用「地区」，火山控制台用「地域」；AK/SK 在火山叫「访问密钥」，bucket 叫「存储桶」，方舟的 `ep-…` 叫「推理接入点」 | 07 篇加术语表；任务状态中文名与静态稿 README 待确认项 4 一并定 | 07，全册 | 已修订 |
| C3 | 任务「改」与续跑 | 见 Q5 | 01 §3，03 §2 | 已修订 |
| C4 | 分页形态 | 见 Q6 | 03 §4，07 §2 | 已修订 |
| C5 | 需求：子任务后「最终报告和性能分析都需要同步更新」。文档只说报告重生成 | 性能剖析按模块替换并保留历次记录（延迟 CSV 追加，v1 rejudge 已如此）；Token 总量累计所有尝试（花了就是花了），详情页可按子任务展开 | 06 §6.1，04 §5 | 已修订 |
| C6 | 重试粒度只有「整个模块」。640 条里 5 条 VLM 超时，重跑整个 task_success 不合理；v1 的 `--retry-abstained` 就是只补判失败条目 | `retry` 默认只重跑所选模块里 `verdict=error` 或因调用失败而弃权的 episode；模块整体失败才全量重跑 | 03 §2，00 §4.1 | 已修订 |
| C7 | 删除任务是否删 TOS 产物没写 | 默认只删平台记录，产物保留；`?purge_artifacts=true` 需二次确认，且不动 `human-decisions/` | 03 §8 | 已修订 |
| C8 | 日志没有持久化与查看入口，SSE 还会限流丢弃 | 见 B18 | 03，06 §1 | 已修订 |
| C9 | SQLite 没有备份方案；主密钥丢失 = 全部密钥作废 | 每日 `VACUUM INTO` 到数据卷并上传到可配置的 TOS 前缀；恢复步骤写进 README | 09 | 已修订 |
| C10 | 「本地挂载路径 experimental」可以读 Pod 内任意路径 | 限定在 `CURATOR_LOCAL_DATA_ROOT` 白名单根目录下，默认关闭 | 00 §5，07 §3 | 已修订 |
| C11 | 需求文档里贴着明文的 TOS AK/SK 和方舟 Key | 测试凭证只经本地 `.env`（已 gitignore）或 K8s Secret 注入，不进仓库、不进设计文档；建议项目收尾后轮换这组密钥 | 10 §5 | 已修订 |
| C12 | 集成测试「8 条小数据集，CI 每次跑」没说数据从哪来，CI 没有 TOS 和方舟凭证 | 仓库内放一个脚本生成的迷你 LeRobot v2 数据集（合成视频），VLM 用录制回放的假后端；真数据真模型只在手动对账里用 | 10 §5 | 已修订 |

## 5. 默认处理的产品细节（已按默认写入，汇总在 00 篇 §7.1；不同意可否掉）

| # | 事项 | 默认 |
|-|-|-|
| D-1 | 同时运行的任务数 | 默认 1（v1 也是「有任务在跑」时禁用按钮），其余排队；`maxRunningTasks` 可调，>1 时 Daemon 按任务数均分 VLM 并发预算（子进程架构下进程内信号量管不到跨进程） |
| D-2 | 429/5xx 自适应降并发放在哪层 | CLI 进程内的 VLM 客户端（Daemon 管不到运行中子进程的闸门），Daemon 只给初值并采集事件 |
| D-3 | 深链带多个数据集 | 新建页进入批量模式：共用一套配置，一次创建 N 个任务，交付目录各自为 `<根>/<数据集>-<MMDD>` |
| D-4 | CPU 档并发 | CLI 默认 1（需求原话）；Daemon planner 默认 `min(8, 核数/4)`，记为 D18 并注明与需求「可以默认用 1」的关系 |
| D-5 | 进度模型 | `progress` 改为分 stage 的数组（每档独立进度、耗时、预计剩余），对应 v1 任务卡 |
| D-6 | 创建任务的幂等 | `POST /tasks` 也支持 `Idempotency-Key`（Agent 重试最容易重复建任务） |
| D-7 | `/metrics` | 与探针一样豁免鉴权，但只监听集群内端口 |
| D-8 | 任务列表的实时性 | 列表页轮询（5s，仅当有非终态任务），不单独做全局 SSE |
| D-9 | 信号协议 | SIGTERM = 收尾当前 episode 后退出（暂停/优雅停机）；SIGINT = 取消在飞请求立即退出（停止）；超时 SIGKILL |

---

## 6. 第二轮：另一份评审的 14 条问答（2026-09-20）

需求方带来另一位评审者的 14 条问题及答复。逐条对照如下，决策编号接着第一轮（D24 起）。

| 条 | 答复 | 与当时文档的关系 | 记为 | 涉及 |
|-|-|-|-|-|
| 1 | 执行出错的 episode 暂不交付，其它数据继续处理 | **改**。原先照搬 v1：调用失败也算弃权、照常交付 | D24 | 00 §4，02 §3.5–3.6，04 §7，06 §3，10 §3.0 |
| 2 | 正常弃权和标注分歧保留 v1：先交付，同时进人工复核 | 一致 | D24 | 06 §3 |
| 3 | 重试只补跑失败的 episode；成功结果保留；新报告完整生成后再替换旧报告 | 与默认项 P10 一致，转正；「替换」落成结果版本 | D25 | 03 §3.2，06 §1，01 §2.3 |
| 4 | 重启 / 升级后自动从检查点续跑；用户暂停保持、用户停止不恢复；在飞请求可能重复一次，报告记录 | 基本一致，补「重复请求如实记录」 | D26 | 01 §3.2，06 §6.1 |
| 5 | 固化源文件清单与版本指纹；源数据变化则停止并提示重新预检 | **新增** | D27 | 02 §3.3（`snapshot`）、§4（退出码 6），01 §2.3 |
| 6 | 草稿可改全部；开始后只能改名称和备注，其余走「复制为新任务」 | 与 D20 一致，补备注字段；重新绑定访问密钥改为单独动作 | D20 | 01 §2.3，03 §2 |
| 7 | 删除只处理平台记录；「清理交付产物」单独、展示路径、二次确认 | 方向一致，从删除接口的参数改为独立动作 | D28 | 03 §8，07 §4.1 |
| 8 | 每任务独立不可覆盖的 `run_id` 目录；只有完整成功的版本才原子更新 `latest`；同目录发布串行 | 补 `latest` 语义与发布锁 | D29 | 06 §1 |
| 9 | 添加后端时能拉出模型就拉，拉不出就手填 | **改**。去掉评审时加的「内置模型清单」，不引入管理 AK/SK | D8 | 08 §4，07 §7，01 §2.2 |
| 10 | planner 自动定计划；API / CLI / UI 可设 CPU、VLM 并发、超时、重试的上限 | **改**。原先写的是「没有任何并发类的键」 | D31 | 03 §3.1，04 §2，07 §3，09 §2.1 |
| 11 | 本期实现框架就好，先不改 | 与 D23 一致 | D23 | — |
| 12 | 补跑全部恢复后任务显示「已完成」，历史留在时间线 | **改**。原先父任务状态不变 | D25 | 01 §2.5、§3，07 §4 |
| 13 | 未验证的密钥可保存并标记；开始任务前输入读、输出写、VLM 最小调用三项必须通过 | **改**。原先是「二次确认」 | D30 | 03 §3，08 §4，07 §3、§7 |
| 14 | 裁决不跨任务，每个任务管好自己的数据集 | **改，改动最大**。原先照搬 v1 的「随交付目录跨批次沿用」；`delivery` 绑定表与 `adjudication_applied` 一并拿掉 | D32 | 01 §2.7，06 §1、§5.3，07 §6，10 §2.3 |

## 7. 第三轮：`review-catalog.md` 的 F01–F28（针对 rev 13）

这份清单写于第一轮修订之前，逐条核对现状。「已覆盖」指第一、二轮已经处理；「本轮补」是这次才补上的。

| 编号 | 现状 | 处理 |
|-|-|-|
| F01 | 本轮补 | 第一轮改成了「review ⊂ passed」，仍不准确：v1 的复核视图里也有被拒的条目（可复议的）。改为质量判决 / 执行完整性 / 人工复核三个维度独立表达（06 §3） |
| F02 | 本轮补 | 模块状态加 `completed_with_errors`；`check` 的输出带逐状态计数，Daemon 不靠退出码 0 判成功（02 §3.5，01 §2.4） |
| F03 | 已覆盖 | A5，`autolabel` |
| F04 | 已覆盖，本轮补一句 | A17；补充 profile 里声明的内容（单位、旋转表示、夹爪极性、相机角色）（05 §3.1） |
| F05 | 已覆盖，本轮补 | A3；补「留遍历顺序里最先出现的一条，因此不许并发」（05 §1） |
| F06 | 本轮补 | 技能画像的归类文本是**标注优先**、caption 兜底，caption 只作分歧检出的另一端；补全 归纳→自查合并→归族→补漏→翻译→分歧→复检 全流程（05 §5） |
| F07 | 本轮补 | 写明提案（planner）→ 执行（`check` 进程内）→ 回执（写进结果）三步各在哪（04 §4.2） |
| F08 | 已覆盖 | D18，不再有帧缓存 |
| F09 | 已覆盖，本轮补 | 上限取交集（D31）；补「子任务同样占运行名额」「对冲与重试在闸门之内」（04 §2.3） |
| F10 | 本轮补 | 确实会双计。改为实际调用账与分摊账两本账，总量只算前者；拿不到 usage 的请求单独计数（01 §2.6，04 §5） |
| F11 | 本轮补 | 判决清单与报告合成不可变的「结果版本」，`commit.json` 最后写，库里 `result_rev` 用 CAS 切换（06 §1，01 §2.3） |
| F12 | 已覆盖，本轮补 | 补跑从出错的那一档往后跑；补 `input_digest` 记录输入集合，数据集级模块据此判断过期（01 §2.4） |
| F13 | 本轮补 | `delivery_stale` 改为按导出指纹计算，只改标也算过期（06 §4，01 §2.3） |
| F14 | 已覆盖 | D27 |
| F15 | 已覆盖 | D32：不跨任务；追加式保留修改历史 |
| F16 | 已覆盖，本轮补 | 补一段「检查点就是三样东西」（01 §3.2） |
| F17 | 本轮补 | 降低 OOM 隔离的承诺；`oom_score_adj`、RSS 准入、检查点续跑（00 §2.1，09 §2.1） |
| F18 | 已覆盖，本轮补 | StatefulSet、去掉迁移 Job、备份；补凭证行的 `key_version`，轮换可续做（08 §2.1） |
| F19 | 本轮补 | 保存时只验身份，读写权限在任务开始前按具体资源验；输入输出两组独立的环境变量（08 §4，02 §2） |
| F20 | 已覆盖 | D8 |
| F21 | 本轮补 | 任务启动时固化 VLM 有效配置（`vlm_snapshot`），只有 API Key 实时取（01 §2.3） |
| F22 | 已覆盖，本轮补 | 补交付目录写探针接口、PATCH 的 `If-Match`（03 §2） |
| F23 | 已覆盖 | D20、D25 |
| F24 | 本轮补 | 事件带 `epoch-seq` 序号；断档发 `reset` 后拉快照；usage / progress 发累计值（03 §5） |
| F25 | 本轮补 | 明细表生成报告时写成 Parquet，Daemon 直接读，不再每页拉起 CLI；游标绑定结果版本（03 §6） |
| F26 | 已覆盖 | A13 |
| F27 | 已覆盖，本轮补 | D19 双基线 + 噪声底；补「回放对账」作为 VLM 三项的首选口径，v3 导出路径与去重需要夹具（10 §3.1–3.2） |
| F28 | 本轮补 | 00 篇新增 §9 需求覆盖表；有出入的三处标 ⚠ |

## 8. 第二、三轮之后追问的 4 个问题（均已答复）

| 问题 | 答复 | 落在 |
|-|-|-|
| 某条 episode 在某一档出错后，还跑不跑后面的档 | 停在出错那一档，补跑时从那一档接着往后跑；整个模块失败时例外，后面的模块照跑、全部条目待补跑 | P10，04 §7 |
| 不参与判废的模块（技能画像、去重）出错，是否也暂不交付 | 统一规则，也暂不交付 | P11，06 §3 |
| v3 格式的对账怎么补 | 本期不做 v3 对账，缺口写明，靠 v1 单元测试和一个不比对的 v3 夹具 | 10 §3.1，11 W7 |
| 00 篇 §7.1 的默认项 | 全部接受，转为已确认 | 00 §7.1 |


## 9. 开工前核对（2026-09-20）

开工前对照 v1 代码核对 W0 依赖的假设，提了 4 个问题，并修了几处文档里的过时说法。

| 问题 | 事实 | 答复 | 落在 |
|-|-|-|-|
| 基线在哪跑 | 解码时的 swscale 转换与 `cv2.resize` 跨 CPU 架构不保证逐位一致；`umi_640_notask` 只在部署环境里有 | 现网 v1 的 Pod；导出脚本自带冻结点的 v1 源码 | 10 §3.1 |
| 搬运与基线以哪个提交为准 | release_v1 仍在合代码：09-19 合入 #157（同步检查改用 direct 法互相关），#155（mcap / lance 读取）尚未合入 | 冻结点定在 `45bdf9292`，之后分批同步；新格式按 `.rrd` 的办法处理 | D34，10 §1 |
| 「执行出错」怎么划线 | v1 大量失败走降级：打分失败后复核可救回、仲裁失败维持弃权、单机位解码失败少一路照判、补 caption 失败给空串照跑 | 宽口径：只要有一次调用重试用尽或一路解码失败就算出错，整条重跑 —— 降级得来的结论证据不全，重跑可能不同 | D33，02 §3.5，04 §7，05 §1，06 §3，10 §3.0 |
| 需求方进度 | — | 同意删除上游 daft 源码与不再需要的代码；静态稿风格已确认 | 10 §1，07 §10，11 §3 |

同时定下的默认做法（未被否决）：W0 的工具先做，基线生成期间 W1、W2 照常推进，卡点改为「W3 动 B 类代码之前基线必须存档」；
基线跑批要求零失败，有失败就「回放 + 补录」再跑一遍；W0 自检增加「v1 录制 → 回放」一致性；
噪声底用 droid 50 条两遍、umi 前 64 条两遍；存档放 TOS，仓库只放清单与 sha256。

顺手修掉的过时说法：03 §2、07 §7、11 §5（W8）仍写「保存 TOS 访问密钥时做写探针」，与 08 §4「保存只验身份」冲突；
00 §7 决策表在 D23 与 D24 之间有空行，D24 之后的行渲染不成表格。
`aggregate` / `report` 的命令行缺 `--revision` 参数（02 §3.6、§3.8），留给 W2 冻结 C2 时补。

## 10. 契约冻结核对（2026-09-21）

W2 冻结五份契约后，给需求方一页摘要（`docs/contracts/SUMMARY.md`），列出冻结时定下的 7 处细节和 1 处待拍板的问题；同日还定了黄金基线的安排。

| 问题 | 事实 | 答复 | 落在 |
|-|-|-|-|
| 已被确定拒绝的条目又有模块出错，进不进 `held` | 设计原文是「任一已勾选模块出错即进 held」；冻结的判决行契约写的是「硬门已确定失败就直接拒绝」，两处不一致。按原文，解码失败、算法异常这类每次都复现的错误会让一条本该拒绝的条目一直卡在待补跑，报告里看不到真正的拒绝理由 | 同意推荐方案：拒绝理由来自正常判完的模块（某个硬门确定失败，或所有已勾选的软分模块都给了分、加权分低于阈值）就直接拒绝，出错的模块照样记在该条目上；重试只补跑 `held`；任务终态看有没有整体失败的模块和待补跑的条目 | D35，00 §4，01 §2.4、§2.5、§3，02 §3.6，03 §3.2，04 §7，06 §3，契约 `cli/verdict-line.schema.json` |
| 黄金基线什么时候生成 | 原定 W3 改 B 类代码之前两个基线必须存档（§9）；基线要在现网 Pod 里跑 | 需求方：v1 在 Pod 上跑过、结果正常，旧系统有备份，暂不生成。卡点放宽：拆编排改由合成数据集上冻结点 v1 与 v2 的逐位对账守住（进 CI），真实数据的基线在 F4.2 黄金对账之前补 | 11 §3 |
| 冻结时定下的 7 处细节 | C1 放 `contracts/` 不放 A 类的 `registry/`；判定结果加 `scored`；证据模式取值用 v1 的 `off`；`aggregate` / `report` 加 `--revision`、报告类接口加 `?rev=N`；新增执行时间线接口；`report.json` 不存 URL；被去重剔除的条目只在 `reject` | 全部接受 | `docs/contracts/SUMMARY.md` §二 |

同时定下的默认做法（未被否决）：planner 库与 VLM 合并框架放 `backend/curation/planner/`，CLI 的 `plan` 命令和 Daemon
调同一个库（02 §3.2），不再单设 `backend/daemon/planner/`；并行 agent 不改公共文件，需要改的写进交付报告，合并时统一落地（11 §6）。

## 11. 静态稿评审（2026-09-21）

需求方看过六页静态稿：页面没有问题，文案问题等全部页面实现之后一次性修改。
待确认 10 条的答复逐条记在 `frontend/mockups/README.md` 的「评审结论」，其中三条成了决策：

| 问题 | 答复 | 落在 |
|-|-|-|
| 导航要不要「数据集」「概览」 | 两个都加。开始质检任务时要核对指纹，确认数据集和添加时一样；不一样就弹框确认，然后重新预检 | D36、D37；01 §2.8，03 §3、§12，05 §3，07 §2–§4 |
| 新建任务单页还是分步 | 两屏：第一屏同现稿，是基本且必填的输入；第二屏针对已开启的模块给出各自的额外输入（必填 / 选填），然后创建 | D38；07 §3 |
| 列表要不要质检模块列 | 要，但模块会有十几、二十个，问怎么展示。按推荐做：只放摘要（项数 + 异常提示 + 预设名），悬停看完整清单，另加按模块筛选 | 07 §4.1 |

其余七条是界面叫法与呈现：状态「部分错误」改叫「错误」；系统暂停保留标签，「系统将自动恢复」做成悬停提示；
日志保持英文；人工裁决以 episode 为索引，卡片写明来源模块；复议按钮用 v1 的「恢复为可用 / 维持拒绝」；
「待裁决 10」与裁决页的进展不冲突，保持现状；从没导出过的叫「导出」。落在 07 §4、§6、§8，06 §5.1。

同时定下的默认做法（未被否决）：侧栏依次为概览、数据集、质检任务、密钥与资源，默认页是概览；
新增的页面和新建任务的第二屏不再补静态稿，直接做 React；`GET /datasets` 改为已登记的数据集列表，
原来的「列 TOS 前缀下的数据集」挪到 `GET /datasets/browse`，随契约 1.1.0 修订落地（03 §12）。
